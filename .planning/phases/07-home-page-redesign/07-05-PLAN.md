---
phase: 07-home-page-redesign
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/home/VideoReels.tsx
  - frontend/src/messages/es.json
  - frontend/src/messages/en.json
  - frontend/src/messages/fr.json
  - frontend/src/messages/de.json
autonomous: true
requirements:
  - HOME-07
  - HOME-08

must_haves:
  truths:
    - "Video reels section shows 4 vertical columns at 9:16 aspect ratio"
    - "Each reel has a thumbnail/placeholder with centered play button overlay"
    - "Clicking a reel opens external link (Instagram/YouTube) in new tab"
    - "'VER TODOS LOS VIDEOS' link button appears in section header"
    - "Section works with placeholder thumbnails until real video content is added"
  artifacts:
    - path: "frontend/src/components/home/VideoReels.tsx"
      provides: "Vertical video reels grid with 9:16 ratio thumbnails + play overlays"
      contains: "VideoReels"
  key_links:
    - from: "frontend/src/components/home/VideoReels.tsx"
      to: "frontend/src/lib/directus.ts"
      via: "WebVideo type from Directus"
      pattern: "WebVideo"
    - from: "frontend/src/components/home/VideoReels.tsx"
      to: "frontend/src/messages/{locale}.json"
      via: "translations prop"
      pattern: "translations"
---

<objective>
Create a new VideoReels component showing 4 vertical video thumbnails at 9:16 aspect ratio with play button overlays and a "VER TODOS LOS VIDEOS" header link.

Purpose: Replace the existing VideoSection (4:3 landscape cards) with vertical reel-style thumbnails matching the v2.0 design reference. Videos open external links (Instagram/YouTube) in new tabs.
Output: New VideoReels client component.
</objective>

<execution_context>
@C:/Users/Piensaenweb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Piensaenweb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-home-page-redesign/07-CONTEXT.md
@frontend/src/components/home/VideoSection.tsx
@frontend/src/lib/types.ts
@frontend/src/lib/directus.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VideoReels component with 9:16 vertical thumbnails</name>
  <files>
    frontend/src/components/home/VideoReels.tsx
  </files>
  <action>
Create `frontend/src/components/home/VideoReels.tsx` as a new client component ("use client").

**Do NOT delete VideoSection.tsx** — it will be unreferenced when page.tsx is rewired in plan 07-06.

**Props interface:**
```typescript
interface VideoReelsTranslations {
  title: string;
  watchVideo: string;
  viewAll: string;
}

interface VideoReelsProps {
  videos: WebVideo[];
  translations: VideoReelsTranslations;
}
```

**Section layout (from CONTEXT.md):**
- Dark green gradient background matching existing VideoSection: `linear-gradient(180deg, #1a3a2a 0%, #1f4433 100%)`.
- Keep the topographic contour pattern overlay from VideoSection.
- Full-width with `py-20 lg:py-28`.

**Section header:**
- Top subtitle line: `VIVAZ CLAY TARGETS` with accent-colored decorative lines on each side (same pattern as current VideoSection header).
- Title: `translations.title` in large white text.
- **"VER TODOS LOS VIDEOS" link button** (HOME-08): Position to the right of the title on desktop, below on mobile. Render as a `<Link>` or `<a>` tag:
  - Text: `translations.viewAll`
  - Style: `text-accent text-[14px] font-semibold uppercase tracking-[2px] hover:text-white transition-colors`
  - Add a right arrow icon (`→` or SVG arrow) after the text.
  - Link destination: `https://www.instagram.com/vivaz_claytargets/` (external, opens in new tab). Per CONTEXT.md deferred ideas, inline video is deferred, so this links to the Instagram profile.
  - Use `target="_blank" rel="noopener noreferrer"`.

**Video grid:**
- 4-column grid on desktop: `grid grid-cols-2 lg:grid-cols-4 gap-4 lg:gap-6`.
- On mobile: 2x2 grid (Claude's discretion from CONTEXT.md).

**Reel card (ReelCard sub-component):**
- **Aspect ratio:** `aspect-[9/16]` for vertical reel format.
- **Thumbnail:** If `video.thumbnail` exists, use `assetUrl(video.thumbnail, { width: 400, format: "webp" })` via `<img>` tag (same pattern as current VideoSection — client component uses `<img>` not `next/image`). If no thumbnail, show a placeholder with `Video` text centered on dark background.
- **Play button overlay:** Centered play button (accent-colored circle with white play icon). Use similar play icon as current VideoSection. Style: `h-14 w-14 rounded-full bg-accent/90 shadow-lg` with hover scale effect.
- **Dark overlay:** `bg-black/30` over thumbnail, lightens on hover.
- **Rounded corners:** `rounded-[16px] overflow-hidden`.
- **Click behavior:** `window.open(video.video_url, "_blank", "noopener,noreferrer")` — same as current VideoSection.
- **Title below card:** Video title in `text-warm-white/90 text-[13px] font-medium mt-2`.
- **Accessibility:** `<button>` element with `aria-label={watchVideoLabel: videoTitle}`.

**Empty state:** If `videos.length === 0`, render the section with 4 placeholder cards (static, no click). Show a muted placeholder with the play button overlay to demonstrate the layout. This handles the CONTEXT.md note: "Placeholders for now — build layout with placeholder thumbnails, real videos added later."

**Imports:** `WebVideo` from `@/lib/types`, `assetUrl`, `IMG_PRESETS` from `@/lib/directus`.
  </action>
  <verify>
    <automated>cd frontend && npx next lint --quiet 2>&1 | head -20</automated>
    <manual>VideoReels component renders (will be wired in 07-06) — check with placeholder state</manual>
  </verify>
  <done>VideoReels component renders 4-column 9:16 vertical thumbnails with play overlays, dark green gradient background, section header with "VER TODOS LOS VIDEOS" link, and graceful placeholder state for empty videos</done>
</task>

<task type="auto">
  <name>Task 2: Add video reels translation keys to all 4 locales</name>
  <files>
    frontend/src/messages/es.json
    frontend/src/messages/en.json
    frontend/src/messages/fr.json
    frontend/src/messages/de.json
  </files>
  <action>
Add a `viewAll` key to the `home.videos` namespace in all 4 locale files:

- es: `home.videos.viewAll`: "Ver todos los videos"
- en: `home.videos.viewAll`: "View all videos"
- fr: `home.videos.viewAll`: "Voir toutes les videos"
- de: `home.videos.viewAll`: "Alle Videos ansehen"

Keep existing `home.videos.title` and `home.videos.watchVideo` unchanged.

Important: Only add the `viewAll` key to the `home.videos` namespace. Do not touch any other translation keys.
  </action>
  <verify>
    <automated>cd frontend && node -e "const es=require('./src/messages/es.json'); const en=require('./src/messages/en.json'); console.log('es viewAll:', es.home.videos.viewAll || 'MISSING'); console.log('en viewAll:', en.home.videos.viewAll || 'MISSING'); console.log('es title kept:', es.home.videos.title ? 'OK' : 'MISSING')"</automated>
  </verify>
  <done>All 4 locale files have home.videos.viewAll key, existing title and watchVideo keys preserved</done>
</task>

</tasks>

<verification>
- `frontend/src/components/home/VideoReels.tsx` exists as a client component
- VideoReels renders 4-column grid with `aspect-[9/16]` ratio
- Play button overlays visible on each reel card
- "VER TODOS LOS VIDEOS" link renders in section header
- Placeholder state works when videos array is empty
- All 4 locale files have `home.videos.viewAll` key
- `npx next lint` passes
</verification>

<success_criteria>
VideoReels section displays 4 vertical reel thumbnails at 9:16 ratio with play button overlays, dark green gradient background, "VER TODOS LOS VIDEOS" link in header, and graceful placeholder/empty state. Clicking a reel opens external video link in new tab.
</success_criteria>

<output>
After completion, create `.planning/phases/07-home-page-redesign/07-05-SUMMARY.md`
</output>
