---
phase: 07-home-page-redesign
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/home/ProductFeature.tsx
  - frontend/src/components/ui/ProductBadge.tsx
  - frontend/src/messages/es.json
  - frontend/src/messages/en.json
  - frontend/src/messages/fr.json
  - frontend/src/messages/de.json
autonomous: true
requirements:
  - HOME-05
  - HOME-06

must_haves:
  truths:
    - "NATURA section shows side-by-side layout: text left, product image right"
    - "ECO STAR section shows alternating layout: product image left, text right"
    - "Each section has a ProductBadge with eco-friendly leaf icon and badge text"
    - "Each section has heading, description paragraph, and CTA button to product page"
    - "Product images come from Directus CMS via assetUrl()"
  artifacts:
    - path: "frontend/src/components/home/ProductFeature.tsx"
      provides: "Side-by-side product feature section with badge, heading, description, CTA, image"
      contains: "ProductFeature"
    - path: "frontend/src/components/ui/ProductBadge.tsx"
      provides: "Reusable badge component with icon + text"
      contains: "ProductBadge"
  key_links:
    - from: "frontend/src/components/home/ProductFeature.tsx"
      to: "frontend/src/lib/directus.ts"
      via: "assetUrl(imageUuid) for Directus product image"
      pattern: "assetUrl"
    - from: "frontend/src/components/home/ProductFeature.tsx"
      to: "frontend/src/messages/{locale}.json"
      via: "useTranslations"
      pattern: "useTranslations"
---

<objective>
Create NATURA and ECO STAR side-by-side feature sections with ProductBadge component, heading, description, CTA, and Directus product images.

Purpose: Replace the existing ProductShowcase component with a new ProductFeature component that follows the v2.0 design reference — side-by-side layout with badge, heading, description paragraph, CTA button, and close-up product image from Directus.
Output: New ProductFeature component + ProductBadge reusable UI component.
</objective>

<execution_context>
@C:/Users/Piensaenweb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Piensaenweb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-home-page-redesign/07-CONTEXT.md
@frontend/src/components/home/ProductShowcase.tsx
@frontend/src/lib/directus.ts
@frontend/src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProductBadge reusable component</name>
  <files>
    frontend/src/components/ui/ProductBadge.tsx
  </files>
  <action>
Create a new reusable `ProductBadge` component at `frontend/src/components/ui/ProductBadge.tsx`.

Design from CONTEXT.md: "ProductBadge (eco-friendly leaf icon + text), used across both NATURA and ECO STAR sections."

Implementation:
```typescript
interface ProductBadgeProps {
  text: string;
  variant?: "nature" | "eco";
  className?: string;
}
```

- Render as a `<span>` with inline-flex, items-center, gap-2.
- Include a small leaf SVG icon (inline, ~16x16px). Use a simple leaf shape: `<svg viewBox="0 0 16 16"><path d="M8 1C4 4 2 8 2 12c2-1 4-2 6-4 2 2 4 3 6 4 0-4-2-8-6-11z" fill="currentColor"/></svg>`. The leaf icon should be `text-current` to inherit color from variant.
- Variant styles:
  - `nature`: `bg-nature/20 text-primary` (green tones)
  - `eco`: `bg-accent/10 text-accent` (accent tones)
- Text styling: `text-[13px] font-semibold uppercase tracking-[2px]`
- Rounded pill shape: `rounded-full px-4 py-2`
- Default export.
  </action>
  <verify>
    <automated>cd frontend && npx next lint --quiet 2>&1 | head -20</automated>
  </verify>
  <done>ProductBadge component renders a pill-shaped badge with leaf icon and text, supports nature/eco variant styling</done>
</task>

<task type="auto">
  <name>Task 2: Create ProductFeature component replacing ProductShowcase</name>
  <files>
    frontend/src/components/home/ProductFeature.tsx
    frontend/src/messages/es.json
    frontend/src/messages/en.json
    frontend/src/messages/fr.json
    frontend/src/messages/de.json
  </files>
  <action>
Create a new `ProductFeature` component at `frontend/src/components/home/ProductFeature.tsx` that replaces the existing `ProductShowcase`.

**Do NOT delete ProductShowcase.tsx** — it will be removed when page.tsx is rewired in plan 07-06.

**Props interface:**
```typescript
interface ProductFeatureProps {
  variant: "natura" | "ecostar";
  imageUuid: string;   // Directus asset UUID from product_lines / pim_products
}
```

The `imageUuid` prop receives a Directus asset UUID fetched from the `pim_products` collection by the parent server component (page.tsx, wired in plan 07-06). This honors the CONTEXT.md decision: "Product images — Directus product_lines collection (CMS-managed)".

**Implementation:**
- Use `useTranslations(variant)` to get the same translation namespace as current ProductShowcase (keys: `badge`, `title`, `description`, `cta`).
- **Layout:** Two-column grid (`grid md:grid-cols-2 gap-12 items-center`).
  - NATURA (`isNatura`): content column LEFT, image column RIGHT.
  - ECO STAR: image column LEFT, content column RIGHT.
  - Use the same conditional rendering pattern as current ProductShowcase.

- **Content column:**
  - `<ProductBadge text={t("badge")} variant={isNatura ? "nature" : "eco"} className="mb-6" />`
  - `<h2>` with `t("title")` — `text-[28px] md:text-[36px] font-bold leading-tight text-primary`
  - `<p>` with `t("description")` — `mt-4 max-w-[475px] font-body text-[18px] leading-[28px] text-primary`
  - `<Button>` with `t("cta")` linking to `/productos/${isNatura ? "natura-standard" : "eco-star-standard"}` — `mt-8 self-start`

- **Image column (Directus CMS — per CONTEXT.md Decision #4):**
  - Build the image URL using `assetUrl(imageUuid, { width: 702, format: "webp" })` from `@/lib/directus`.
  - Wrap in `<div className="group relative overflow-hidden rounded-[20px]">`.
  - Use `next/image` with `src={assetUrl(imageUuid, { width: 702, format: "webp" })}` and `width={702} height={478} sizes="(max-width: 768px) 100vw, 50vw"`. Set `unoptimized` since the Directus transform already handles optimization.
  - Add hover zoom: `transition-transform duration-700 group-hover:scale-[1.03]`.
  - Alt text: `t("title")`.

- **Section wrapper:** `<section className={isNatura ? "bg-cream py-20" : "bg-white py-20"}>` with `<Container>`.

- **Animations:** Wrap content in `<InView animation={isNatura ? "slide-in-left" : "slide-in-right"}>` and image in `<InView animation={isNatura ? "slide-in-right" : "slide-in-left"}>`.

- **Imports needed:** `useTranslations` from `next-intl`, `Image` from `next/image`, `Container`, `Button`, `InView`, `ProductBadge`, `assetUrl` from `@/lib/directus`.

**Note for plan 07-06 (page.tsx wiring):** The parent page.tsx must fetch product images from Directus and pass `imageUuid` to each ProductFeature. Use `getProducts({ range: "Premium Natura", featured: true, limit: 1 })` to get the NATURA product image UUID (`product.image`), and `getProducts({ range: "Eco Star Efficiency", featured: true, limit: 1 })` for ECO STAR. Example:
```tsx
const [naturaProducts, ecostarProducts] = await Promise.all([
  getProducts({ range: "Premium Natura", featured: true, limit: 1 }),
  getProducts({ range: "Eco Star Efficiency", featured: true, limit: 1 }),
]);
<ProductFeature variant="natura" imageUuid={naturaProducts[0]?.image ?? ""} />
<ProductFeature variant="ecostar" imageUuid={ecostarProducts[0]?.image ?? ""} />
```

**Translations:** The existing `natura.badge` and `ecostar.badge` keys already exist in locale files. Verify they contain appropriate eco badge text (e.g., "100% RESINA DE PINO" for natura, "EFICIENCIA ECOLOGICA" for ecostar). If badge keys are missing or need updating per the design reference, add/update them in all 4 locales:
- es: natura.badge = "100% Resina de Pino", ecostar.badge = "Eficiencia Ecologica"
- en: natura.badge = "100% Pine Resin", ecostar.badge = "Ecological Efficiency"
- fr: natura.badge = "100% Resine de Pin", ecostar.badge = "Efficacite Ecologique"
- de: natura.badge = "100% Kiefernharz", ecostar.badge = "Okologische Effizienz"

Important: Only modify the `natura` and `ecostar` namespace keys if needed. Do not touch any other translation keys.
  </action>
  <verify>
    <automated>cd frontend && npx next lint --quiet 2>&1 | head -20</automated>
    <manual>Verify ProductFeature renders side-by-side layout at localhost:3030 when imported (will be wired in 07-06)</manual>
  </verify>
  <done>ProductFeature component renders NATURA (content left, image right) and ECO STAR (image left, content right) with ProductBadge, heading, description, CTA, and Directus product image via assetUrl(imageUuid). Alternating background colors (cream/white).</done>
</task>

</tasks>

<verification>
- `frontend/src/components/ui/ProductBadge.tsx` exists with leaf icon + text + variant styling
- `frontend/src/components/home/ProductFeature.tsx` exists with side-by-side layout
- ProductFeature supports both `natura` and `ecostar` variants with alternating column order
- ProductBadge used in ProductFeature for both variants
- All 4 locale files have correct badge keys for natura/ecostar namespaces
- `npx next lint` passes
</verification>

<success_criteria>
NATURA and ECO STAR each have a side-by-side feature section with ProductBadge (eco-friendly leaf icon + text), heading, description, CTA button to product page, and product image. Sections alternate layout direction. ProductBadge is reusable across both sections.
</success_criteria>

<output>
After completion, create `.planning/phases/07-home-page-redesign/07-04-SUMMARY.md`
</output>
