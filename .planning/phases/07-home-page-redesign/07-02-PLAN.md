---
phase: 07-home-page-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/home/ProductRangeStrip.tsx
  - frontend/src/messages/es.json
  - frontend/src/messages/en.json
  - frontend/src/messages/fr.json
  - frontend/src/messages/de.json
autonomous: true
requirements:
  - HOME-03

must_haves:
  truths:
    - "ProductRangeStrip displays 3 product lines: NATURA, ECO STAR, and VIVAZ RANGE"
    - "Each product line shows product images (clay target cutouts) and a label"
    - "Crosshair decorations appear between columns on desktop"
    - "Section is responsive: stacks vertically on mobile with single column"
  artifacts:
    - path: "frontend/src/components/home/ProductRangeStrip.tsx"
      provides: "3-column product range strip with NATURA, ECO STAR, VIVAZ RANGE"
      contains: "vivazRange"
  key_links:
    - from: "frontend/src/components/home/ProductRangeStrip.tsx"
      to: "frontend/src/messages/{locale}.json"
      via: "useTranslations('range')"
      pattern: "useTranslations.*range"
---

<objective>
Update ProductRangeStrip to display 3 product lines (NATURA, ECO STAR, VIVAZ RANGE) with product images and crosshair decorations.

Purpose: The existing ProductRangeStrip already shows 3 product lines with clay chip images and crosshair decorations. This plan ensures it matches the v2.0 design reference exactly and that all translations are correct.
Output: Updated ProductRangeStrip component matching the design reference.
</objective>

<execution_context>
@C:/Users/Piensaenweb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Piensaenweb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-home-page-redesign/07-CONTEXT.md
@frontend/src/components/home/ProductRangeStrip.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review and refine ProductRangeStrip component for v2.0 design</name>
  <files>
    frontend/src/components/home/ProductRangeStrip.tsx
  </files>
  <action>
The current ProductRangeStrip already has 3 product lines (natura, ecostar, vivazRange) with clay chip images, crosshair decorations (CornerMirilla, SeparatorMirilla), and labels. Review and adjust to ensure it matches the CONTEXT.md design reference:

1. **Verify existing structure matches design:** The current component has:
   - 3 columns for NATURA, ECO STAR, VIVAZ RANGE
   - Clay chip images from `/img/vivaz-clay-*` files
   - CornerMirilla and SeparatorMirilla crosshair SVG decorations
   - Labels using `useTranslations("range")`

   This already aligns with HOME-03 requirements. Check that:
   - The crosshair decorations use the `target-oscuro.svg` style (dark crosshairs on light background). The current inline SVGs use `#075627` strokes which is correct for the dark crosshair style.
   - Product images are appropriately sized and positioned.

2. **Add "VER PRODUCTOS" link to each product line label.** Per the design reference, each product line should be clickable, linking to its product page:
   - NATURA label links to `/productos/natura-standard`
   - ECO STAR label links to `/productos/eco-star-standard`
   - VIVAZ RANGE label links to `/productos` (no specific page yet, link to products index)

   Wrap each label `<p>` in a `Link` from `@/i18n/navigation`. Style the label with `hover:text-accent transition-colors` for interaction feedback.

3. **Add InView animation** to the section for scroll-triggered entrance. Wrap the grid in `<InView animation="fade-in-up">` for consistent animation with other sections.

4. Keep the existing responsive layout (grid-cols-1 on mobile, md:grid-cols-3 on desktop). Keep the `bg-cream-light` background and `-mt-36` negative margin overlap.
  </action>
  <verify>
    <automated>cd frontend && npx next lint --quiet 2>&1 | head -20</automated>
    <manual>Check ProductRangeStrip shows 3 product lines with clickable labels at localhost:3030</manual>
  </verify>
  <done>ProductRangeStrip shows 3 product lines (NATURA, ECO STAR, VIVAZ RANGE) with clay chip images, crosshair decorations, clickable labels linking to product pages, and scroll-triggered entrance animation</done>
</task>

<task type="auto">
  <name>Task 2: Verify and update range translation keys in all 4 locales</name>
  <files>
    frontend/src/messages/es.json
    frontend/src/messages/en.json
    frontend/src/messages/fr.json
    frontend/src/messages/de.json
  </files>
  <action>
Verify the `range` namespace in all 4 locale files has the correct keys:
- `range.natura`: "NATURA" (same in all locales — product brand name)
- `range.ecostar`: "ECO STAR" (same in all locales — product brand name)
- `range.vivazRange`: "VIVAZ RANGE" (same in all locales — product brand name)

These are product line names so they should be identical across all locales (brand names not translated).

Check that these keys exist in all 4 files. If any are missing, add them. If the keys already exist with correct values, no changes needed.

Important: Only modify the `range` namespace keys. Do not touch any other translation keys.
  </action>
  <verify>
    <automated>cd frontend && node -e "const es=require('./src/messages/es.json'); const en=require('./src/messages/en.json'); const fr=require('./src/messages/fr.json'); const de=require('./src/messages/de.json'); const check = (l,n) => { const r = l.range; return r.natura && r.ecostar && r.vivazRange ? 'OK' : 'MISSING'; }; console.log('es:', check(es,'es'), 'en:', check(en,'en'), 'fr:', check(fr,'fr'), 'de:', check(de,'de'))"</automated>
  </verify>
  <done>All 4 locale files have correct range.natura, range.ecostar, and range.vivazRange keys with brand name values</done>
</task>

</tasks>

<verification>
- ProductRangeStrip.tsx renders 3 columns with NATURA, ECO STAR, VIVAZ RANGE
- Crosshair decorations visible on desktop view
- Each product label links to appropriate product page
- All 4 locale files have range namespace keys
- `npx next lint` passes
</verification>

<success_criteria>
ProductRangeStrip displays 3 product lines with product images, crosshair decorations, clickable labels, and scroll animation. All 4 locales have correct brand name keys.
</success_criteria>

<output>
After completion, create `.planning/phases/07-home-page-redesign/07-02-SUMMARY.md`
</output>
