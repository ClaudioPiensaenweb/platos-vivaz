---
phase: 07-home-page-redesign
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/home/WhyVivazGrid.tsx
  - frontend/src/messages/es.json
  - frontend/src/messages/en.json
  - frontend/src/messages/fr.json
  - frontend/src/messages/de.json
autonomous: true
requirements:
  - HOME-04

must_haves:
  truths:
    - "WhyVivaz section displays 'POR QUE VIVAZ' heading"
    - "3 redesigned image cards show: Calidad, Sostenibilidad, Experiencia"
    - "Each card has a large image top and green card body with title, divider, description"
    - "Cards have hover zoom effect on images and scroll-triggered entrance animation"
  artifacts:
    - path: "frontend/src/components/home/WhyVivazGrid.tsx"
      provides: "3-card grid: Calidad, Sostenibilidad, Experiencia"
      contains: "whyVivaz"
  key_links:
    - from: "frontend/src/components/home/WhyVivazGrid.tsx"
      to: "frontend/src/messages/{locale}.json"
      via: "useTranslations('whyVivaz')"
      pattern: "useTranslations.*whyVivaz"
---

<objective>
Redesign WhyVivazGrid cards to match the v2.0 design reference with 3 image cards (Calidad, Sostenibilidad, Experiencia).

Purpose: Update the existing WhyVivaz section to match the new brand design with larger images, refined card styling, and updated copy referencing the three core pillars.
Output: Updated WhyVivazGrid component with redesigned cards.
</objective>

<execution_context>
@C:/Users/Piensaenweb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Piensaenweb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-home-page-redesign/07-CONTEXT.md
@frontend/src/components/home/WhyVivazGrid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update WhyVivazGrid component cards to match v2.0 design</name>
  <files>
    frontend/src/components/home/WhyVivazGrid.tsx
  </files>
  <action>
The current WhyVivazGrid already has 3 cards with images, green card bodies, and titles. The CONTEXT.md specifies 3 redesigned cards: Calidad, Sostenibilidad, Experiencia. Update the component:

1. **Update pillars data array.** The current cards use keys `quality`, `sustainability`, `performance`. The design reference says "Calidad, Sostenibilidad, Experiencia". Map these:
   - `quality` stays → "Calidad" (translation already handles this)
   - `sustainability` stays → "Sostenibilidad" (translation handles this)
   - `performance` → rename key to `experience` and update image reference. This reflects "Experiencia" from the design.

   Update the pillars array:
   ```typescript
   const pillars = [
     { key: "quality", descKey: "qualityDesc", image: "/img/why-quality.png", ... },
     { key: "sustainability", descKey: "sustainabilityDesc", image: "/img/why-sustainability.png", ... },
     { key: "experience", descKey: "experienceDesc", image: "/img/why-performance.png", ... },
   ];
   ```

2. **Refine card styling** per design reference:
   - Keep the image-on-top + green-card-body layout.
   - Increase image aspect ratio slightly: add `aspect-[16/10]` class to the image wrapper `div` to ensure consistent height.
   - Keep `rounded-b-[30px]` on card wrapper.
   - Keep the green card body (`bg-nature`) with centered text.
   - Keep the `group-hover:scale-[1.08]` zoom effect on images.
   - Add `overflow-hidden rounded-t-[30px]` to image wrapper for consistent top rounding matching the card.

3. **Section header.** Keep the existing SectionHeader with `t("subtitle")` and `t("title")`. The CONTEXT.md says "POR QUE VIVAZ" heading which maps to `whyVivaz.title`.

4. **Animations.** Keep existing InView animations: `fade-in-up` on header, `scale-in` with staggered delays on cards.

5. Keep `bg-white py-20` section background.
  </action>
  <verify>
    <automated>cd frontend && npx next lint --quiet 2>&1 | head -20</automated>
    <manual>Check WhyVivaz section shows 3 cards with images and green bodies at localhost:3030</manual>
  </verify>
  <done>WhyVivazGrid renders 3 cards (Calidad, Sostenibilidad, Experiencia) with image top, green card body, hover zoom, and scroll animation</done>
</task>

<task type="auto">
  <name>Task 2: Update whyVivaz translation keys for "Experiencia" in all 4 locales</name>
  <files>
    frontend/src/messages/es.json
    frontend/src/messages/en.json
    frontend/src/messages/fr.json
    frontend/src/messages/de.json
  </files>
  <action>
Update the `whyVivaz` namespace in all 4 locale files:

1. Rename `performance` → `experience` and `performanceDesc` → `experienceDesc`:
   - es: `experience`: "Experiencia", `experienceDesc`: "Mas de 50 anos de trayectoria en la fabricacion de platos de tiro, con presencia en los principales campeonatos y campos de tiro del mundo."
   - en: `experience`: "Experience", `experienceDesc`: "Over 50 years of trajectory in clay target manufacturing, with a presence at the world's leading championships and shooting grounds."
   - fr: `experience`: "Experience", `experienceDesc`: "Plus de 50 ans de savoir-faire dans la fabrication de plateaux de tir, avec une presence lors des grands championnats et stands de tir du monde."
   - de: `experience`: "Erfahrung", `experienceDesc`: "Uber 50 Jahre Erfahrung in der Herstellung von Wurfscheiben, mit Prasenz bei den fuhrenden Meisterschaften und Schiessanlagen weltweit."

2. Keep existing `quality`, `qualityDesc`, `sustainability`, `sustainabilityDesc` keys unchanged.

3. Remove old `performance` and `performanceDesc` keys from all 4 files.

4. Keep `whyVivaz.title` and `whyVivaz.subtitle` unchanged.

Important: Only modify the `whyVivaz` namespace keys. Do not touch any other translation keys.
  </action>
  <verify>
    <automated>cd frontend && node -e "const es=require('./src/messages/es.json'); console.log('experience:', es.whyVivaz.experience || 'MISSING'); console.log('experienceDesc:', es.whyVivaz.experienceDesc ? 'OK' : 'MISSING'); console.log('old performance:', es.whyVivaz.performance ? 'STILL EXISTS - FAIL' : 'removed - OK')"</automated>
  </verify>
  <done>All 4 locale files have updated whyVivaz keys with experience/experienceDesc replacing performance/performanceDesc, old keys removed</done>
</task>

</tasks>

<verification>
- WhyVivazGrid.tsx uses `experience` key (not `performance`)
- 3 cards render with correct images and green card bodies
- All 4 locale files have `whyVivaz.experience` and `whyVivaz.experienceDesc`
- No locale file has leftover `whyVivaz.performance` or `whyVivaz.performanceDesc`
- `npx next lint` passes
</verification>

<success_criteria>
WhyVivaz section displays "POR QUE VIVAZ" heading with 3 redesigned image cards (Calidad, Sostenibilidad, Experiencia). Each card has an image with hover zoom, green body with title + description, and scroll entrance animation. All 4 locales updated.
</success_criteria>

<output>
After completion, create `.planning/phases/07-home-page-redesign/07-03-SUMMARY.md`
</output>
