---
phase: 07-home-page-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/public/img/hero-forest.jpg
  - frontend/src/components/home/HeroSection.tsx
  - frontend/src/messages/es.json
  - frontend/src/messages/en.json
  - frontend/src/messages/fr.json
  - frontend/src/messages/de.json
autonomous: true
requirements:
  - HOME-01
  - HOME-02

must_haves:
  truths:
    - "Hero section displays forest background image covering full viewport"
    - "White crosshair SVG decorations appear on the hero (desktop only)"
    - "Hero text reads 'VIVAZ CLAY TARGETS' as heading with 'LIDERES EN PLATOS ECOLOGICOS' subtitle line"
    - "A descriptive paragraph and CTA button linking to /productos appear below the heading"
    - "Hero is responsive: text stacks centered on mobile, crosshairs hidden on small screens"
  artifacts:
    - path: "frontend/public/img/hero-forest.jpg"
      provides: "Forest background image for hero"
    - path: "frontend/src/components/home/HeroSection.tsx"
      provides: "Redesigned hero component with forest bg + crosshairs"
      contains: "hero-forest.jpg"
  key_links:
    - from: "frontend/src/components/home/HeroSection.tsx"
      to: "frontend/public/img/hero-forest.jpg"
      via: "Image src prop"
      pattern: "hero-forest"
    - from: "frontend/src/components/home/HeroSection.tsx"
      to: "frontend/src/messages/{locale}.json"
      via: "useTranslations('hero')"
      pattern: "useTranslations.*hero"
---

<objective>
Redesign the home page hero section with a forest background image, crosshair SVG decorative elements, and updated text hierarchy.

Purpose: Replace the current hero background (hero-bg.png + topographic overlay) with the new forest photograph and provided crosshair SVGs to match the updated brand direction.
Output: Redesigned HeroSection component + forest background image in public/img.
</objective>

<execution_context>
@C:/Users/Piensaenweb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Piensaenweb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-home-page-redesign/07-CONTEXT.md
@frontend/src/components/home/HeroSection.tsx
@briefing/v2/target-hero.svg
@briefing/v2/marca-esquina.svg
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy forest hero image and redesign HeroSection component</name>
  <files>
    frontend/public/img/hero-forest.jpg
    frontend/src/components/home/HeroSection.tsx
  </files>
  <action>
1. Copy `briefing/v2/foto-cabecera.jpg` to `frontend/public/img/hero-forest.jpg`.

2. Rewrite `frontend/src/components/home/HeroSection.tsx`:
   - **Background:** Replace `/img/hero-bg.png` with `/img/hero-forest.jpg`. Keep the `next/image` fill pattern with `priority`, `sizes="100vw"`, `quality={90}`.
   - **Overlay:** Replace the topographic SVG overlay with a simple dark gradient overlay (`bg-gradient-to-b from-black/60 via-black/30 to-black/60`). Remove the `/svg/hero-overlay.svg` reference entirely.
   - **Crosshair decorations:** Replace the inline Mirilla SVG with actual crosshair asset usage. Read `briefing/v2/target-hero.svg` (white crosshair, 39x40px) and embed as an inline SVG component `CrosshairHero`. Position 2-3 instances decoratively at different corners/edges using absolute positioning. Use `hidden lg:block` to hide on mobile. Also read `briefing/v2/marca-esquina.svg` (green corner mark, 20x20px) and embed as `CornerMark` — position at 2-4 corners of the content area for brand decoration, also `hidden lg:block`.
   - **Text hierarchy:** Keep using `useTranslations("hero")`. The structure should be:
     - Subtitle line: `t("subtitle")` ("VIVAZ CLAY TARGETS") — styled as small uppercase tracking text with accent dot, similar to current.
     - Main heading (h1): `t("title")` — large bold white text, same responsive sizing as current (`text-3xl md:text-5xl lg:text-[72px]`).
     - Description paragraph: Use geo-routed `t("descriptionNational")` / `t("descriptionExport")` via the `market` prop (keep existing pattern).
     - CTA button: `t("cta")` linking to `/productos` via `MagneticButton` + `Button` (keep existing pattern).
   - **Layout:** Keep the existing full-viewport section with `-mt-[130px]` negative margin for navbar overlap. Content centered vertically and horizontally.
   - **Animations:** Keep existing `animate-fade-in-up` + delay classes on text elements.
   - Keep `market` prop interface and geo-routing logic unchanged.

3. Do NOT modify `page.tsx` in this plan — page assembly happens in plan 07-06.
  </action>
  <verify>
    <automated>cd frontend && npx next lint --quiet 2>&1 | head -20</automated>
    <manual>Visually inspect hero at localhost:3030 — forest background visible, crosshairs on desktop, text readable</manual>
  </verify>
  <done>HeroSection renders forest background with crosshair decorations, correct text hierarchy (VIVAZ CLAY TARGETS heading + subtitle + description + CTA), responsive layout with crosshairs hidden on mobile, hero-forest.jpg exists in public/img</done>
</task>

<task type="auto">
  <name>Task 2: Update hero translation keys for all 4 locales</name>
  <files>
    frontend/src/messages/es.json
    frontend/src/messages/en.json
    frontend/src/messages/fr.json
    frontend/src/messages/de.json
  </files>
  <action>
Review and update the `hero` namespace in all 4 locale files. The CONTEXT.md specifies:
- `hero.subtitle`: "VIVAZ CLAY TARGETS" (same in all locales — brand name)
- `hero.title`: "LIDERES EN PLATOS ECOLOGICOS" in Spanish. Check current value matches the CONTEXT.md reference. The context says the heading should read "VIVAZ CLAY TARGETS / LIDERES EN PLATOS ECOLOGICOS". The current implementation has subtitle = "VIVAZ CLAY TARGETS" and title = "LIDERES EUROPEOS EN PLATOS ECOLOGICOS". Update the Spanish title to "LIDERES EN PLATOS ECOLOGICOS" (removing "EUROPEOS" per design reference). Update equivalent translations in en/fr/de accordingly:
  - en: "LEADERS IN ECOLOGICAL CLAY TARGETS"
  - fr: "LEADERS DES PLATEAUX ECOLOGIQUES"
  - de: "FUHRER BEI OKOLOGISCHEN WURFSCHEIBEN"
- Keep `hero.description`, `hero.descriptionNational`, `hero.descriptionExport`, `hero.cta` unchanged — they already work.

Note: CSS text-transform: uppercase is applied globally to headings per Phase 6, so store text in natural case in JSON. The subtitle "VIVAZ CLAY TARGETS" is a brand name so keep it uppercase in the source.

Important: Only modify the `hero` namespace keys. Do not touch any other translation keys.
  </action>
  <verify>
    <automated>cd frontend && node -e "const es=require('./src/messages/es.json'); const en=require('./src/messages/en.json'); console.log('ES title:', es.hero.title); console.log('EN title:', en.hero.title); console.log(es.hero.subtitle === 'VIVAZ CLAY TARGETS' ? 'PASS' : 'FAIL')"</automated>
  </verify>
  <done>All 4 locale files have updated hero.title matching the CONTEXT.md design reference (no "EUROPEOS"), subtitle remains "VIVAZ CLAY TARGETS"</done>
</task>

</tasks>

<verification>
- `frontend/public/img/hero-forest.jpg` exists and is a valid JPEG
- HeroSection.tsx references `hero-forest.jpg`, not `hero-bg.png`
- HeroSection.tsx contains crosshair SVG decorations with `hidden lg:block`
- All 4 locale files have consistent hero namespace keys
- `npx next lint` passes without errors
</verification>

<success_criteria>
Hero section displays the forest background photograph with crosshair decorative elements, correct heading text ("VIVAZ CLAY TARGETS" + localized "leaders in ecological clay targets"), geo-routed description paragraph, and CTA button. Crosshairs are desktop-only. All 4 locale translations are updated.
</success_criteria>

<output>
After completion, create `.planning/phases/07-home-page-redesign/07-01-SUMMARY.md`
</output>
