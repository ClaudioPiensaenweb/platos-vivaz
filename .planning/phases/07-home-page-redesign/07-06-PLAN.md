---
phase: 07-home-page-redesign
plan: 06
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
  - 07-03
  - 07-04
  - 07-05
files_modified:
  - frontend/public/img/contact-cta-bg.jpg
  - frontend/src/components/home/ContactCTABanner.tsx
  - frontend/src/components/home/ContactFormSection.tsx
  - frontend/src/app/[locale]/page.tsx
  - frontend/src/messages/es.json
  - frontend/src/messages/en.json
  - frontend/src/messages/fr.json
  - frontend/src/messages/de.json
autonomous: false
requirements:
  - HOME-09
  - HOME-10

must_haves:
  truths:
    - "Contact CTA banner displays dark green gradient background with product photo left and heading + CTA button right"
    - "Contact form section has two-column layout: address data left, 5-field form right"
    - "Contact form has exactly 5 fields: Nombre, Apellidos, Email, Telefono, Mensaje"
    - "Home page renders all sections in correct order: Hero, ProductRangeStrip, WhyVivaz, ProductFeature(natura), ProductFeature(ecostar), VideoReels, ContactCTABanner, ContactFormSection"
    - "CommitmentBanner and ProductShowcase are no longer rendered on home page"
  artifacts:
    - path: "frontend/src/components/home/ContactCTABanner.tsx"
      provides: "Dark green gradient CTA banner with product photo + heading + CTA"
      contains: "ContactCTABanner"
    - path: "frontend/src/components/home/ContactFormSection.tsx"
      provides: "Two-column contact section: address left, form right"
      contains: "ContactFormSection"
    - path: "frontend/src/app/[locale]/page.tsx"
      provides: "Assembled home page with all v2.0 sections"
      contains: "ProductFeature"
    - path: "frontend/public/img/contact-cta-bg.jpg"
      provides: "Sustainability product photo for CTA banner"
  key_links:
    - from: "frontend/src/app/[locale]/page.tsx"
      to: "frontend/src/components/home/ContactCTABanner.tsx"
      via: "import and render"
      pattern: "ContactCTABanner"
    - from: "frontend/src/app/[locale]/page.tsx"
      to: "frontend/src/components/home/ContactFormSection.tsx"
      via: "import and render"
      pattern: "ContactFormSection"
    - from: "frontend/src/app/[locale]/page.tsx"
      to: "frontend/src/components/home/ProductFeature.tsx"
      via: "import and render"
      pattern: "ProductFeature"
    - from: "frontend/src/app/[locale]/page.tsx"
      to: "frontend/src/components/home/VideoReels.tsx"
      via: "import and render"
      pattern: "VideoReels"
    - from: "frontend/src/components/home/ContactFormSection.tsx"
      to: "frontend/src/components/contact/ContactForm.tsx"
      via: "import reused form component"
      pattern: "ContactForm"
---

<objective>
Create Contact CTA banner and Contact form section components, then assemble the complete v2.0 home page by wiring all new section components into page.tsx.

Purpose: Complete the home page redesign by adding the contact CTA + form blocks and replacing old components (CommitmentBanner, ProductShowcase, VideoSection) with the new ones (ProductFeature, VideoReels, ContactCTABanner, ContactFormSection).
Output: ContactCTABanner + ContactFormSection components, fully assembled home page.
</objective>

<execution_context>
@C:/Users/Piensaenweb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Piensaenweb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-home-page-redesign/07-CONTEXT.md
@.planning/phases/07-home-page-redesign/07-01-SUMMARY.md
@.planning/phases/07-home-page-redesign/07-04-SUMMARY.md
@.planning/phases/07-home-page-redesign/07-05-SUMMARY.md
@frontend/src/app/[locale]/page.tsx
@frontend/src/components/contact/ContactSplit.tsx
@frontend/src/components/contact/ContactForm.tsx
@frontend/src/components/contact/ContactCards.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ContactCTABanner and ContactFormSection components</name>
  <files>
    frontend/public/img/contact-cta-bg.jpg
    frontend/src/components/home/ContactCTABanner.tsx
    frontend/src/components/home/ContactFormSection.tsx
    frontend/src/messages/es.json
    frontend/src/messages/en.json
    frontend/src/messages/fr.json
    frontend/src/messages/de.json
  </files>
  <action>
**1. Copy CTA banner image:**
Copy `briefing/IMG/vivaz-clay-targets-sustainability.jpg` to `frontend/public/img/contact-cta-bg.jpg`.

**2. Create ContactCTABanner** at `frontend/src/components/home/ContactCTABanner.tsx`:

Server Component (no "use client" — no interactivity). Uses `getTranslations` from `next-intl/server`.

**Layout from CONTEXT.md:** Dark green gradient background. Product/sustainability photo on left side. Heading + CTA button on right side. Full-width section.

Implementation:
- Section wrapper: `bg-gradient-to-r from-primary-dark to-primary py-16 lg:py-24`.
- Use `Container` for content bounds.
- Two-column grid: `grid md:grid-cols-2 gap-8 items-center`.
  - Left column: Product image using `next/image` with `src="/img/contact-cta-bg.jpg"`, `width={600} height={400}`, rounded corners `rounded-[20px]`, `sizes="(max-width: 768px) 100vw, 50vw"`.
  - Right column:
    - Heading (`h2`): Use `t("contactCta.title")` translation key. Style: `text-[28px] md:text-[36px] font-bold text-white leading-tight`.
    - Description paragraph: `t("contactCta.description")`. Style: `mt-4 font-body text-[18px] text-white/80 max-w-md`.
    - CTA Button: `<Button href="#contacto" variant="accent" size="lg">{t("contactCta.cta")}</Button>`. Use an anchor link `#contacto` that scrolls to the contact form section below.
- Wrap in `InView animation="fade-in"` for scroll entrance.

**3. Create ContactFormSection** at `frontend/src/components/home/ContactFormSection.tsx`:

Server Component. Uses `getTranslations` from `next-intl/server` and `headers` for geo-routing.

**Layout from CONTEXT.md:** Two-column: left has address block with Nacional/Internacional data; right has the 5-field contact form with submit button.

Implementation:
- Section wrapper: `<section id="contacto" className="bg-cream py-20 lg:py-28">` (the `id="contacto"` provides the anchor target for the CTA banner).
- Use `Container`.
- Two-column grid: `grid gap-12 lg:grid-cols-2`.
  - Left column (address data):
    - Heading: `t("talkTitle")` styled as `text-[24px] font-bold text-primary`.
    - Description: `t("talkDesc")`.
    - Then render `ContactCards` component from `@/components/contact/ContactCards` with `layout="stacked"` and geo-highlighted market. Pass the same translations as current ContactSplit does.
  - Right column (contact form):
    - Heading: `t("formTitle")` in `text-[20px] font-bold text-primary`.
    - Render `<ContactForm />` from `@/components/contact/ContactForm`.
    - Wrap in a white card: `rounded-[24px] bg-white p-8 shadow-sm lg:p-10`.

The 5 fields (Nombre, Apellidos, Email, Telefono, Mensaje) are already in the existing ContactForm component. The existing form also has `market` select and `privacyConsent` checkbox which should remain. The CONTEXT.md says "Form fields: Nombre, Apellidos, Email, Telefono, Mensaje" — the existing form matches this (plus market + privacy which are functional necessities).

**4. Add translation keys** to `home` or a new `contactCta` namespace in all 4 locales:

Add to the `home` namespace a new `contactCta` sub-object:
- es: `home.contactCta.title`: "Contacta con nosotros", `home.contactCta.description`: "Descubre como VIVAZ puede ser tu proveedor de platos de tiro ecologicos. Estamos listos para ayudarte.", `home.contactCta.cta`: "Contactar"
- en: `home.contactCta.title`: "Get in touch", `home.contactCta.description`: "Discover how VIVAZ can be your supplier of ecological clay targets. We are ready to help.", `home.contactCta.cta`: "Contact us"
- fr: `home.contactCta.title`: "Contactez-nous", `home.contactCta.description`: "Decouvrez comment VIVAZ peut etre votre fournisseur de plateaux de tir ecologiques. Nous sommes prets a vous aider.", `home.contactCta.cta`: "Nous contacter"
- de: `home.contactCta.title`: "Kontaktieren Sie uns", `home.contactCta.description`: "Entdecken Sie, wie VIVAZ Ihr Lieferant fur okologische Wurfscheiben sein kann. Wir helfen Ihnen gerne.", `home.contactCta.cta`: "Kontakt"

Important: Only add/modify keys in the `home.contactCta` namespace. Do not touch other translation keys.
  </action>
  <verify>
    <automated>cd frontend && npx next lint --quiet 2>&1 | head -20</automated>
    <manual>Check ContactCTABanner and ContactFormSection render correctly at localhost:3030</manual>
  </verify>
  <done>ContactCTABanner renders dark green gradient with product photo + heading + CTA. ContactFormSection renders two-column layout with address data left and 5-field form right. contact-cta-bg.jpg copied to public/img. Translation keys added for all 4 locales.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble v2.0 home page in page.tsx</name>
  <files>
    frontend/src/app/[locale]/page.tsx
  </files>
  <action>
Rewrite `frontend/src/app/[locale]/page.tsx` to wire all v2.0 section components.

**Target structure (from CONTEXT.md):**
```tsx
<HeroSection market={market} />
<ProductRangeStrip />
<WhyVivazGrid />
<ProductFeature variant="natura" />
<ProductFeature variant="ecostar" />
<VideoReels videos={videos} translations={videoReelsTranslations} />
<ContactCTABanner />
<ContactFormSection />
```

**Changes from current page.tsx:**
1. **Remove imports:** `ProductShowcase`, `CommitmentBanner`, `VideoSection`.
2. **Add imports:** `ProductFeature` from `@/components/home/ProductFeature`, `VideoReels` from `@/components/home/VideoReels`, `ContactCTABanner` from `@/components/home/ContactCTABanner`, `ContactFormSection` from `@/components/home/ContactFormSection`.
3. **Remove:** `ContactSplit` import (replaced by ContactFormSection).
4. **Update VideoReels translations object:** Add `viewAll: t("videos.viewAll")` to the translations prop.
5. **Keep:** `headers()` for market detection, `getVideos()` for video data, `getTranslations("home")` for translations.

**Full rewrite of return JSX:**
```tsx
return (
  <>
    <HeroSection market={market} />
    <ProductRangeStrip />
    <WhyVivazGrid />
    <ProductFeature variant="natura" />
    <ProductFeature variant="ecostar" />
    <VideoReels videos={videos} translations={videoReelsTranslations} />
    <ContactCTABanner />
    <ContactFormSection />
  </>
);
```

Where `videoReelsTranslations` is:
```typescript
const videoReelsTranslations = {
  title: t("videos.title"),
  watchVideo: t("videos.watchVideo"),
  viewAll: t("videos.viewAll"),
};
```

**Do NOT delete old component files** (ProductShowcase.tsx, CommitmentBanner.tsx, VideoSection.tsx, CTASection.tsx, ColorStrip.tsx) — they may be referenced elsewhere or can be cleaned up in a future phase. Just remove them from page.tsx imports.
  </action>
  <verify>
    <automated>cd frontend && npx next build 2>&1 | tail -30</automated>
    <manual>Visit localhost:3030 and verify all home page sections render in correct order</manual>
  </verify>
  <done>Home page renders all v2.0 sections in correct order: Hero (forest bg), ProductRangeStrip (3 lines), WhyVivaz (3 cards), ProductFeature(natura), ProductFeature(ecostar), VideoReels (vertical 9:16), ContactCTABanner (dark green gradient), ContactFormSection (address + form). Old CommitmentBanner and ProductShowcase no longer appear.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete v2.0 home page</name>
  <files>frontend/src/app/[locale]/page.tsx</files>
  <action>
Human visual verification checkpoint. What was built: Complete v2.0 home page with all 8 sections: forest hero with crosshairs, 3-line product range strip, WhyVivaz redesigned cards, NATURA + ECO STAR feature sections with badges, vertical video reels, contact CTA banner, and contact form section.

How to verify:
1. Visit http://localhost:3030 in desktop browser
2. Verify hero section: forest background, crosshair decorations visible, "VIVAZ CLAY TARGETS" heading, subtitle, description, CTA button
3. Scroll to ProductRangeStrip: 3 columns (NATURA, ECO STAR, VIVAZ RANGE) with clay target images and crosshair decorations
4. Scroll to WhyVivaz: 3 image cards (Calidad, Sostenibilidad, Experiencia) with green bodies and hover zoom
5. Scroll to NATURA section: badge left, heading, description, CTA, product image right
6. Scroll to ECO STAR section: product image left, badge right, heading, description, CTA (alternating layout)
7. Scroll to VideoReels: dark green background, 4 vertical columns (9:16), play overlays, "VER TODOS LOS VIDEOS" link
8. Scroll to ContactCTABanner: dark green gradient, product photo left, heading + CTA right
9. Scroll to Contact form: address data left, 5-field form right
10. Resize to mobile (375px): verify all sections stack vertically, crosshairs hidden, text readable
11. Switch locale to /en and verify English translations throughout

Resume signal: Type "approved" or describe issues to fix.
  </action>
  <verify>User approves visual result or provides feedback for iteration</verify>
  <done>User has approved the complete v2.0 home page visual result across desktop and mobile viewports</done>
</task>

</tasks>

<verification>
- `frontend/public/img/contact-cta-bg.jpg` exists
- ContactCTABanner.tsx renders dark green gradient CTA with product photo
- ContactFormSection.tsx renders two-column contact layout
- page.tsx imports and renders all 8 v2.0 section components
- page.tsx does NOT import CommitmentBanner, ProductShowcase, or VideoSection
- All 4 locale files have home.contactCta keys
- `npx next build` succeeds with no errors
- Home page renders correctly in browser
</verification>

<success_criteria>
Complete v2.0 home page assembled with all 8 sections in correct order. Contact CTA banner shows dark green gradient with product photo and heading. Contact form section shows address data + 5-field form. No v1.0-only components (CommitmentBanner, ProductShowcase) rendered. Build succeeds. User approves visual result.
</success_criteria>

<output>
After completion, create `.planning/phases/07-home-page-redesign/07-06-SUMMARY.md`
</output>
