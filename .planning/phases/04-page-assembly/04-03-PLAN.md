---
phase: 04-page-assembly
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/[locale]/tecnologia/page.tsx
  - frontend/src/app/[locale]/regulacion-2026/page.tsx
  - frontend/src/app/[locale]/noticias/page.tsx
  - frontend/src/app/[locale]/noticias/[slug]/page.tsx
  - frontend/src/messages/es.json
  - frontend/src/messages/en.json
  - frontend/src/messages/fr.json
  - frontend/src/messages/de.json
autonomous: true
requirements:
  - SUST-02
  - SUST-04
  - REG-01
  - REG-02
  - REG-03
  - REG-05
  - BLOG-01
  - BLOG-02
  - BLOG-03

must_haves:
  truths:
    - "Technology page explains pine resin chemistry with PAHComparisonChart replacing text-based comparison"
    - "Technology page displays CertBadgeRow in the REACH section"
    - "Technology page renders TimelineSection with 1967/2001/2026 milestones"
    - "Regulation page renders in structure: countdown timer -> ComplianceMatrix -> PAHComparisonChart"
    - "Regulation page has external link to EUR-Lex EU 2025/660 document"
    - "Regulation page displays CertBadgeRow below ComplianceMatrix"
    - "Regulation page emphasizes Vivaz 25-year head start via copy and timeline"
    - "Blog listing page renders BlogCard components in 3-column grid from Directus data"
    - "Blog detail page renders BlogHero + BlogContent with sanitized rich text"
    - "Blog pages pass locale to components for localized date formatting"
    - "All new translation keys exist in all 4 locale files"
  artifacts:
    - path: "frontend/src/app/[locale]/tecnologia/page.tsx"
      provides: "Technology page with PAHComparisonChart, CertBadgeRow, TimelineSection"
    - path: "frontend/src/app/[locale]/regulacion-2026/page.tsx"
      provides: "Regulation page with countdown, ComplianceMatrix, PAHChart, EUR-Lex link"
    - path: "frontend/src/app/[locale]/noticias/page.tsx"
      provides: "Blog listing with BlogCard grid"
    - path: "frontend/src/app/[locale]/noticias/[slug]/page.tsx"
      provides: "Blog detail with BlogHero + BlogContent"
  key_links:
    - from: "frontend/src/app/[locale]/tecnologia/page.tsx"
      to: "PAHComparisonChart"
      via: "Replaces text-based comparison section"
      pattern: "PAHComparisonChart"
    - from: "frontend/src/app/[locale]/regulacion-2026/page.tsx"
      to: "ComplianceMatrix + PAHComparisonChart"
      via: "Wired into regulation page between countdown and CTA"
      pattern: "ComplianceMatrix|PAHComparisonChart"
    - from: "frontend/src/app/[locale]/noticias/page.tsx"
      to: "BlogCard"
      via: "Maps over getBlogPosts() result with locale prop"
      pattern: "BlogCard.*locale"
    - from: "frontend/src/app/[locale]/noticias/[slug]/page.tsx"
      to: "BlogHero + BlogContent"
      via: "Renders blog post with hero image and sanitized body"
      pattern: "BlogHero|BlogContent"
---

<objective>
Assemble the Technology page (PAHComparisonChart + CertBadgeRow + TimelineSection), Regulation 2026 page (countdown + ComplianceMatrix + PAHChart + EUR-Lex link), and Blog listing/detail pages (BlogCard grid + BlogHero/BlogContent) with real Directus data, Phase 3 components, and briefing copy.

Purpose: The technology and regulation pages are the authority-building core -- they prove Vivaz's 25-year ecological leadership and position the EU 2025/660 regulation as validating their approach. The blog pages complete the content platform for ongoing communication.

Output: 4 fully assembled pages (Technology, Regulation, Blog listing, Blog detail) with all Phase 3 components wired in.
</objective>

<execution_context>
@C:/Users/Piensaenweb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Piensaenweb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-page-assembly/04-CONTEXT.md
@.planning/phases/04-page-assembly/04-RESEARCH.md
@frontend/src/app/[locale]/tecnologia/page.tsx
@frontend/src/app/[locale]/regulacion-2026/page.tsx
@frontend/src/app/[locale]/noticias/page.tsx
@frontend/src/app/[locale]/noticias/[slug]/page.tsx
@frontend/src/components/technology/PAHComparisonChart.tsx
@frontend/src/components/regulation/ComplianceMatrix.tsx
@frontend/src/components/regulation/CountdownTimer.tsx
@frontend/src/components/blog/BlogCard.tsx
@frontend/src/components/blog/BlogHero.tsx
@frontend/src/components/blog/BlogContent.tsx
@frontend/src/components/about/TimelineSection.tsx
@frontend/src/components/product/CertBadgeRow.tsx
@frontend/src/lib/directus.ts
@frontend/src/messages/es.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Technology and Regulation pages with Phase 3 components</name>
  <files>
    frontend/src/app/[locale]/tecnologia/page.tsx
    frontend/src/app/[locale]/regulacion-2026/page.tsx
    frontend/src/messages/es.json
    frontend/src/messages/en.json
    frontend/src/messages/fr.json
    frontend/src/messages/de.json
  </files>
  <action>
**1. Technology page (`tecnologia/page.tsx`):**
- Read the current page to understand existing structure (it has a comparison section, REACH section, hero, etc.)
- Keep the existing PageHero at top with technology-themed content
- Replace the text-based petroleum vs Vivaz comparison section with `PAHComparisonChart`:
  - Import PAHComparisonChart from `@/components/technology/PAHComparisonChart`
  - Build translations prop with `getTranslations({ locale, namespace: "technology" })`:
    ```typescript
    const pahTranslations = {
      traditional: t("pahChart.traditional"),
      euLimit: t("pahChart.euLimit"),
      ecoStar: t("pahChart.ecoStar"),
      natura: t("pahChart.natura"),
      euLimitLabel: t("pahChart.euLimitLabel"),
      noPatLabel: t("pahChart.noPatLabel"),
      unit: t("pahChart.unit"),
    };
    ```
  - Wrap in a section with heading explaining pine resin chemistry advantage (SUST-02)
- Add `CertBadgeRow` in the REACH/certifications section:
  - Import CertBadgeRow from `@/components/product/CertBadgeRow`
  - Pass certifications array: `["REACH", "ISSF", "ISO 14001", "EU 2025/660"]` (or fetch from data if available)
  - This satisfies SUST-04 (carbon footprint, REACH traceability, regulatory compliance accessible)
- Add `TimelineSection` to show innovation journey (shared with About page):
  - Import TimelineSection from the Phase 3 component location (check actual path -- likely `@/components/about/TimelineSection` or `@/components/shared/TimelineSection`)
  - Build events array with 1967/2001/2026 milestones using translation keys
  - Render between the PAH chart section and the REACH section
- Ensure sections alternate cream/white backgrounds for visual rhythm (DESGN-01 consistency)
- Add technology page translation keys to all 4 locales:
  - `technology.pahChart.*` keys (traditional, euLimit, ecoStar, natura, euLimitLabel, noPatLabel, unit)
  - `technology.resinTitle`, `technology.resinDescription` (pine resin chemistry explanation)
  - `technology.reachTitle`, `technology.reachDescription`
  - `technology.timeline.*` keys (founded, foundedDesc, pioneers, pioneersDesc, regulation, regulationDesc) -- these may be shared with about namespace; check if they already exist under a common namespace

**2. Regulation page (`regulacion-2026/page.tsx`):**
- Read the current page to understand existing structure (uses RegulationContent with countdown, timeline, info cards, CTA)
- The CONTEXT decision specifies structure: countdown timer -> ComplianceMatrix -> PAH chart (three-layer proof)
- Keep existing CountdownTimer (or RegulationContent countdown section) at the top
- After countdown, add `ComplianceMatrix`:
  - Import ComplianceMatrix from `@/components/regulation/ComplianceMatrix`
  - Build translations prop: check what ComplianceMatrix expects (translations-as-props pattern from Phase 3)
  - ComplianceMatrix shows NATURA / ECO STAR / Traditional compliance status in accessible table
- After ComplianceMatrix, add `CertBadgeRow`:
  - Render below ComplianceMatrix per CONTEXT ("CertBadgeRow appears on BOTH Technology page AND Regulation page below ComplianceMatrix")
- After CertBadgeRow, add `PAHComparisonChart`:
  - Same translations pattern as Technology page
  - The chart provides the visual proof layer showing bar comparison
- Add EUR-Lex external link section:
  - URL: `https://eur-lex.europa.eu/legal-content/EN/TXT/?uri=CELEX:32025R0660`
  - Render as a prominent external link with icon: `<a href={eurLexUrl} target="_blank" rel="noopener noreferrer">`
  - Include a plain-language summary paragraph explaining what the regulation means (from translations)
  - Style: accent colored link with external link icon (inline SVG arrow-up-right)
  - Add `regulation.euDocLink`, `regulation.euDocSummary` translation keys
- Emphasize Vivaz's 25-year head start (REG-05):
  - Include a callout section or highlighted paragraph with copy emphasizing: "Since 2001, Vivaz has been producing with pine resin -- 25 years before the regulation requires it"
  - Add `regulation.headStart`, `regulation.headStartDescription` translation keys
- Keep existing CTA section at the bottom
- Add all regulation-specific translation keys to all 4 locales:
  - `regulation.complianceMatrix.*` (check Phase 3 ComplianceMatrix component for expected translation key structure)
  - `regulation.euDocLink`, `regulation.euDocSummary`
  - `regulation.headStart`, `regulation.headStartDescription`
  - `regulation.pahChart.*` (may share with technology namespace)
  </action>
  <verify>
    <automated>cd C:/Users/Piensaenweb/Documents/Claude/platos-vivaz/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Technology page shows PAHComparisonChart + CertBadgeRow + TimelineSection; Regulation page shows countdown + ComplianceMatrix + CertBadgeRow + PAHChart + EUR-Lex link</manual>
  </verify>
  <done>
    - Technology page has PAHComparisonChart replacing text comparison (SUST-02)
    - Technology page has CertBadgeRow in REACH section (SUST-04)
    - Technology page has TimelineSection with innovation milestones
    - Regulation page structure: countdown -> ComplianceMatrix -> CertBadgeRow -> PAHChart (REG-01, REG-02)
    - Regulation page has EUR-Lex external link (REG-03)
    - Regulation page emphasizes 25-year head start (REG-05)
    - All translation keys added to all 4 locale files
  </done>
</task>

<task type="auto">
  <name>Task 2: Blog listing and detail pages with Directus content</name>
  <files>
    frontend/src/app/[locale]/noticias/page.tsx
    frontend/src/app/[locale]/noticias/[slug]/page.tsx
    frontend/src/messages/es.json
    frontend/src/messages/en.json
    frontend/src/messages/fr.json
    frontend/src/messages/de.json
  </files>
  <action>
**1. Blog listing page (`noticias/page.tsx`):**
- Read the current page to understand existing implementation (likely basic article markup)
- Convert to Server Component that fetches blog posts and passes locale
- Fetch data: `const posts = await getBlogPosts();`
- Get translations: `const t = await getTranslations({ locale, namespace: "news" });`
- Import BlogCard from `@/components/blog/BlogCard` (Phase 3 component)
- Replace existing basic article markup with BlogCard grid:
  ```tsx
  <div className="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
    {posts.map(post => (
      <BlogCard
        key={post.id}
        post={post}
        locale={locale}
        readMoreLabel={t("readMore")}
      />
    ))}
  </div>
  ```
- Keep PageHero at top with news/blog section title
- Handle empty state: if no posts, show a friendly "No posts yet" message
- Wrap card grid in InView for scroll-triggered animation
- Add `generateMetadata()` for per-locale SEO tags (news.metaTitle, news.metaDescription)
- This satisfies BLOG-01, BLOG-03 (locale prop passed to BlogCard)

**2. Blog detail page (`noticias/[slug]/page.tsx`):**
- Read the current page to understand existing implementation (likely bare div with prose classes)
- Import BlogHero from `@/components/blog/BlogHero` and BlogContent from `@/components/blog/BlogContent`
- Fetch single post: `const post = await getBlogPostBySlug(slug);` with `if (!post) notFound();`
- Get locale from params: `const { locale, slug } = await params;`
- Replace existing basic layout with Phase 3 components:
  ```tsx
  <>
    <BlogHero post={post} locale={locale} />
    <BlogContent post={post} locale={locale} />
  </>
  ```
- BlogHero renders full-width image + title + date overlay (built in Phase 3)
- BlogContent renders sanitized HTML body with @tailwindcss/typography prose classes (built in Phase 3)
- Both components accept post and locale props for content localization
- Update `generateMetadata()` to use blog post SEO fields (seo_title, seo_description) with locale-aware content selection
- Update `generateStaticParams()` to return slugs from `getBlogPosts()`
- This satisfies BLOG-02, BLOG-03

**3. Translation keys:**
- Add/verify blog-related keys in all 4 locale files:
  - `news.title` (page heading)
  - `news.readMore` (BlogCard CTA text)
  - `news.noPosts` (empty state message)
  - `news.metaTitle`, `news.metaDescription`
  - `news.backToList` (detail page back link)
- Check that existing keys from Phase 3 component development are present; add any missing ones
  </action>
  <verify>
    <automated>cd C:/Users/Piensaenweb/Documents/Claude/platos-vivaz/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Blog listing shows BlogCard grid; Blog detail shows BlogHero + BlogContent with formatted text</manual>
  </verify>
  <done>
    - Blog listing page renders BlogCard components in 3-column responsive grid (BLOG-01)
    - Blog detail page renders BlogHero + BlogContent with sanitized rich text (BLOG-02)
    - Both pages pass locale to components for localized dates (BLOG-03)
    - Empty state handled gracefully
    - generateMetadata provides locale-specific SEO tags
    - All translation keys present in all 4 locale files
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. Technology page renders PAHComparisonChart (not text comparison), CertBadgeRow, TimelineSection
3. Regulation page renders countdown -> ComplianceMatrix -> CertBadgeRow -> PAHChart -> EUR-Lex link
4. EUR-Lex link opens in new tab with correct URL
5. Blog listing renders BlogCard grid from Directus posts
6. Blog detail renders BlogHero + BlogContent with proper prose formatting
7. All pages work in all 4 locales (no missing translation keys)
</verification>

<success_criteria>
- Technology page explains pine resin chemistry with visual PAH comparison (SUST-02)
- Technology page shows REACH traceability and certifications (SUST-04)
- Regulation page is a complete EU 2025/660 explainer (REG-01)
- ComplianceMatrix serves as didactic infographic (REG-02)
- EUR-Lex link present and functional (REG-03)
- 25-year head start emphasized prominently (REG-05)
- Blog listing renders Directus posts as BlogCard grid (BLOG-01)
- Blog detail renders rich text with BlogHero/BlogContent (BLOG-02)
- Blog supports all 4 locales (BLOG-03)
- Zero TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-page-assembly/04-03-SUMMARY.md`
</output>
