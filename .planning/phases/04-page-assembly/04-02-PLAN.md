---
phase: 04-page-assembly
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/[locale]/productos/page.tsx
  - frontend/src/app/[locale]/productos/[slug]/page.tsx
  - frontend/src/components/product/ProductLinePage.tsx
  - frontend/src/components/product/FormatTabs.tsx
  - frontend/src/messages/es.json
  - frontend/src/messages/en.json
  - frontend/src/messages/fr.json
  - frontend/src/messages/de.json
autonomous: true
requirements:
  - PROD-01
  - PROD-02
  - PROD-03
  - PROD-05
  - PROD-06

must_haves:
  truths:
    - "Product listing page (/productos) shows 3-category grid: Premium Natura, Eco Star, Special Formats with enhanced ProductCards (PAH badge, discipline icons, Directus imagery)"
    - "Visiting /productos/natura shows all NATURA format variants (Standard, Rabbit, etc.) as switchable tabs with full specs"
    - "Visiting /productos/eco-star shows all ECO STAR format variants as switchable tabs with full specs"
    - "Product detail page renders TechSpecGrid with PAH level prominently displayed"
    - "Product detail page renders PAHComparisonChart below the specs section"
    - "Product detail page shows LogisticsTable for distributor procurement data"
    - "Product detail page shows CertBadgeRow for certification visual proof"
    - "Catalog PDF download CTA links to Catalog-min.pdf from Directus"
    - "All new translation keys exist in all 4 locale files"
  artifacts:
    - path: "frontend/src/app/[locale]/productos/page.tsx"
      provides: "Product listing with 3-category grid and enhanced cards"
    - path: "frontend/src/app/[locale]/productos/[slug]/page.tsx"
      provides: "Product line page (slug = natura | eco-star) with format variant tabs"
    - path: "frontend/src/components/product/ProductLinePage.tsx"
      provides: "Client Component for tab-based format variant switching"
    - path: "frontend/src/components/product/FormatTabs.tsx"
      provides: "Tab UI component for switching between product format variants"
  key_links:
    - from: "frontend/src/app/[locale]/productos/[slug]/page.tsx"
      to: "getProducts({ range })"
      via: "Server Component fetches all variants for the product line"
      pattern: "getProducts.*range"
    - from: "frontend/src/components/product/ProductLinePage.tsx"
      to: "TechSpecGrid, PAHComparisonChart, LogisticsTable, CertBadgeRow"
      via: "Renders Phase 3 components for the selected variant"
      pattern: "TechSpecGrid|PAHComparisonChart|LogisticsTable|CertBadgeRow"
    - from: "frontend/src/app/[locale]/productos/[slug]/page.tsx"
      to: "generateStaticParams"
      via: "Returns [{slug: 'natura'}, {slug: 'eco-star'}] for line-level slugs"
      pattern: "generateStaticParams"
---

<objective>
Assemble the product listing page (/productos) with enhanced 3-category grid and the product detail pages (/productos/natura, /productos/eco-star) with format variant tabs, TechSpecGrid, PAHComparisonChart, LogisticsTable, CertBadgeRow, and catalog PDF download. The detail page architecture is revised from per-SKU slugs to per-line slugs with inline format variant switching.

Purpose: Product pages are the commercial core -- distributors need complete specs, format variants, logistics data, and certification proof to make purchasing decisions. The tab-based format selector lets a visitor compare all variants within a product line on a single page.

Output: Enhanced product listing page + 2 product line detail pages (natura, eco-star) with tab-based variant switching + all Phase 3 components wired in.
</objective>

<execution_context>
@C:/Users/Piensaenweb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Piensaenweb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-page-assembly/04-CONTEXT.md
@.planning/phases/04-page-assembly/04-RESEARCH.md
@frontend/src/app/[locale]/productos/page.tsx
@frontend/src/app/[locale]/productos/[slug]/page.tsx
@frontend/src/lib/directus.ts
@frontend/src/lib/types.ts
@frontend/src/components/product/TechSpecGrid.tsx
@frontend/src/components/product/LogisticsTable.tsx
@frontend/src/components/product/DisciplineBadge.tsx
@frontend/src/components/product/CertBadgeRow.tsx
@frontend/src/components/technology/PAHComparisonChart.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Product detail page with line-level slugs and format variant tabs</name>
  <files>
    frontend/src/app/[locale]/productos/[slug]/page.tsx
    frontend/src/components/product/ProductLinePage.tsx
    frontend/src/components/product/FormatTabs.tsx
    frontend/src/messages/es.json
    frontend/src/messages/en.json
    frontend/src/messages/fr.json
    frontend/src/messages/de.json
  </files>
  <action>
**1. Revise product detail page architecture (`productos/[slug]/page.tsx`):**
- Read the current page.tsx to understand existing implementation
- Revise the slug convention: slug = product LINE ("natura" | "eco-star"), not individual SKU
- Update `generateStaticParams()` to return `[{ slug: "natura" }, { slug: "eco-star" }]`
- Map slugs to range_category values:
  ```typescript
  const rangeMap: Record<string, string> = {
    "natura": "Premium Natura",
    "eco-star": "Eco Star Efficiency",
  };
  ```
- Fetch ALL products for the line: `const variants = await getProducts({ range: rangeMap[slug] });`
- If no range match or no variants, call `notFound()`
- Build translations object for all child components:
  - TechSpecGrid translations (pahLevel, material, diameter, weight, height, disciplines, certifications, etc.)
  - PAHComparisonChart translations (traditional, euLimit, ecoStar, natura, euLimitLabel, noPatLabel, unit)
  - LogisticsTable translations (boxUnits, palletEu, container20, container40, etc.)
  - General product page translations (downloadCatalog, specifications, logistics, formats, etc.)
- Fetch brand data for catalog PDF: `const brand = await getBrandData();`
- Render `<ProductLinePage variants={variants} translations={...} catalogPdfUrl={assetUrl(brand?.catalog_pdf)} locale={locale} />`
- Update `generateMetadata()` to produce per-line title/description using the `slug` param

**2. ProductLinePage Client Component (`frontend/src/components/product/ProductLinePage.tsx`):**
- `"use client"` component that manages the currently selected format variant
- Props: `variants: Product[]`, `translations: Record<string, string>`, `catalogPdfUrl: string`, `locale: string`
- State: `const [activeIndex, setActiveIndex] = useState(0);`
- `activeVariant = variants[activeIndex]`
- Layout (top to bottom):
  1. Product line hero: large product image (`DirectusImage` or `assetUrl(activeVariant.image, { width: IMG_PRESETS.hero })`), line name, subtitle
  2. `<FormatTabs variants={variants} activeIndex={activeIndex} onSelect={setActiveIndex} />`
  3. `<TechSpecGrid product={activeVariant} translations={techSpecTranslations} />`
  4. PAH section: `<PAHComparisonChart translations={pahTranslations} />`
  5. Catalog PDF download CTA: `<a href={catalogPdfUrl} target="_blank" rel="noopener noreferrer">` styled as accent button
  6. `<CertBadgeRow certifications={activeVariant.certifications ?? []} />`
  7. `<LogisticsTable data={activeVariant.logistics_data} translations={logisticsTranslations} />` (only if logistics_data exists)
- Use `Container` wrapper for consistent max-width
- Import all Phase 3 components: TechSpecGrid, PAHComparisonChart, CertBadgeRow, LogisticsTable

**3. FormatTabs component (`frontend/src/components/product/FormatTabs.tsx`):**
- `"use client"` component for the tab/toggle UI
- Props: `variants: Product[]`, `activeIndex: number`, `onSelect: (index: number) => void`
- Render a horizontal scrollable row of tab buttons, one per variant
- Each tab shows: variant name (e.g., "Standard o110", "Rabbit o110") and diameter badge
- Active tab: bg-primary text-white (or bg-accent depending on line), inactive: bg-cream border
- Tabs must be horizontally scrollable on mobile (`overflow-x-auto flex gap-2`)
- Use locale-aware variant name from translations if available, fallback to product.name

**4. Translation keys:**
- Add `products.downloadCatalog`, `products.specifications`, `products.logistics`, `products.formats`, `products.selectFormat` to all 4 locale files
- Add PAH chart translations under `products.pahChart.*` namespace (traditional, euLimit, ecoStar, natura, euLimitLabel, noPatLabel, unit) -- these may already exist under `pahChart.*` from Phase 3; check before duplicating
- Add logistics table labels under `products.logistics.*` (boxUnits, palletEu, palletEuBoxes, palletEuTotal, container20, container20Pallets, container20Total, container40, container40Pallets, container40Total)
- Ensure TechSpecGrid translation keys exist under `products.*` namespace
  </action>
  <verify>
    <automated>cd C:/Users/Piensaenweb/Documents/Claude/platos-vivaz/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Navigate to /productos/natura -- should see format tabs, TechSpecGrid, PAH chart, logistics table, cert badges, catalog PDF CTA</manual>
  </verify>
  <done>
    - generateStaticParams returns [{slug: "natura"}, {slug: "eco-star"}]
    - Product detail page fetches all variants for the line via getProducts({ range })
    - ProductLinePage renders tab-based variant switcher with TechSpecGrid, PAHComparisonChart, LogisticsTable, CertBadgeRow
    - FormatTabs component provides horizontally scrollable tab UI
    - Catalog PDF download CTA links to Directus-hosted catalog
    - All translation keys added to all 4 locale files
  </done>
</task>

<task type="auto">
  <name>Task 2: Product listing page with enhanced 3-category grid</name>
  <files>
    frontend/src/app/[locale]/productos/page.tsx
    frontend/src/messages/es.json
    frontend/src/messages/en.json
    frontend/src/messages/fr.json
    frontend/src/messages/de.json
  </files>
  <action>
**1. Enhance product listing page (`productos/page.tsx`):**
- Read the current page.tsx to understand existing implementation
- This page must display products in 3 category sections: Premium Natura, Eco Star Efficiency, Special Formats (per CONTEXT: keep the current 3-category grid)
- Fetch all products: `const products = await getProducts();`
- Group by range_category:
  ```typescript
  const natura = products.filter(p => p.range_category === "Premium Natura");
  const ecoStar = products.filter(p => p.range_category === "Eco Star Efficiency");
  const special = products.filter(p => p.range_category === "Special Formats");
  ```
- Render 3 sections, each with a section header (SectionHeader or custom heading) and a grid of product cards
- Each card enhancement (per CONTEXT "enhance each ProductCard with PAH badge, discipline icons, and Directus imagery"):
  - Product image from Directus via `assetUrl(product.image, { width: IMG_PRESETS.card })` using DirectusImage or next/image
  - PAH badge: colored pill showing PAH level ("0 mg/kg" in green for NATURA, "<50 mg/kg" in amber for ECO STAR)
  - Discipline icons: render DisciplineBadge for each linked discipline (show first 3-4, with "+N" overflow if more)
  - Product name and short description
  - CTA link to product line page: `/productos/natura` for Premium Natura products, `/productos/eco-star` for Eco Star products
  - Visual distinction between NATURA (premium feel: gold/green accent border or badge) and ECO STAR (performance: blue/silver accent)
- For Special Formats section: these products may not have their own detail page (they're variants within NATURA or ECO STAR lines) -- link to the parent line page
- Use `Link` from `@/i18n/navigation` for locale-aware routing
- Use `getTranslations({ locale, namespace: "products" })` for section titles and labels
- Add translation keys: `products.premiumNatura`, `products.ecoStar`, `products.specialFormats`, `products.viewLine`, `products.pahBadge0`, `products.pahBadgeCompliant`
- Wrap each section in InView for scroll-triggered fade-in animations

**2. Product listing generateMetadata:**
- Update or add `generateMetadata()` to provide per-locale title/description for the listing page
- Use `products.metaTitle` and `products.metaDescription` translation keys

**3. Visual design consistency:**
- Use cream background for the page
- Each category section has a subtle section divider or background variation (alternating cream/white sections)
- Product cards have rounded corners, subtle shadow on hover, consistent padding
- The NATURA section should feel premium (dark green accents), ECO STAR should feel technical/performance (accent color)
- Corner bracket decorators (from ProductRangeStrip design motif) can be applied around category headers if they exist as a reusable pattern
  </action>
  <verify>
    <automated>cd C:/Users/Piensaenweb/Documents/Claude/platos-vivaz/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Navigate to /productos -- should see 3 category sections with enhanced cards showing PAH badges, discipline icons, and product images</manual>
  </verify>
  <done>
    - Product listing page renders 3 category sections: Premium Natura, Eco Star, Special Formats
    - Each product card shows: Directus image, PAH badge (color-coded), discipline icons, name, description, CTA link
    - Visual distinction between NATURA (premium) and ECO STAR (performance) cards
    - Links navigate to /productos/natura or /productos/eco-star (line-level slugs)
    - All translation keys added to all 4 locale files
    - generateMetadata provides per-locale SEO tags
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `/productos` listing page renders 3 category grids with enhanced cards
3. `/productos/natura` renders format tabs, TechSpecGrid, PAH chart, logistics, certs, catalog CTA
4. `/productos/eco-star` renders same structure with ECO STAR data
5. Format tab switching updates all displayed specs without page navigation
6. Catalog PDF CTA links to a valid Directus asset URL
7. All 4 locale files have identical key sets for product-related translations
</verification>

<success_criteria>
- NATURA product page displays complete specs (PROD-01)
- ECO STAR product page displays complete specs (PROD-02)
- All 6 formats visible across the two product lines (PROD-03)
- Product listing has visual distinction between NATURA and ECO STAR (PROD-05)
- Logistics table visible with format/pack/pallet data (PROD-06)
- Zero TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-page-assembly/04-02-SUMMARY.md`
</output>
