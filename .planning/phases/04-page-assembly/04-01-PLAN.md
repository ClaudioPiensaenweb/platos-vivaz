---
phase: 04-page-assembly
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/[locale]/page.tsx
  - frontend/src/app/[locale]/layout.tsx
  - frontend/src/app/[locale]/sobre-vivaz/page.tsx
  - frontend/src/app/[locale]/contacto/page.tsx
  - frontend/src/components/home/VideoSection.tsx
  - frontend/src/components/home/HeroSection.tsx
  - frontend/src/components/contact/ContactForm.tsx
  - frontend/src/app/api/contact/route.ts
  - frontend/src/lib/directus.ts
  - frontend/src/lib/types.ts
  - frontend/src/components/layout/Footer.tsx
  - frontend/src/messages/es.json
  - frontend/src/messages/en.json
  - frontend/src/messages/fr.json
  - frontend/src/messages/de.json
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05
  - LEAD-01
  - LEAD-02
  - LEAD-03
  - LEAD-04
  - LEAD-06
  - ABOUT-01
  - ABOUT-02
  - SUST-03
  - DESGN-01
  - DESGN-02
  - DESGN-04

must_haves:
  truths:
    - "Home page renders sections in correct order: Hero -> ProductRangeStrip -> WhyVivaz -> NATURA showcase -> ECO STAR showcase -> VideoSection -> CommitmentBanner -> ContactSplit -> Footer"
    - "Hero shows geo-adapted copy via market prop from Server Component parent"
    - "VideoSection renders 4 video cards from Directus web_videos collection with thumbnail and play overlay"
    - "ContactSplit renders inline on home page with Nacional/Internacional split and contact form"
    - "Standalone /contacto page reuses ContactSplit with side-by-side card layout on desktop"
    - "Contact form has simplified fields: name, apellidos, email, phone, market select, message, privacy checkbox"
    - "WhatsAppFAB appears globally on all pages via locale layout"
    - "About page renders TimelineSection with 1967/2001/2026 milestones and SVG icon value cards (no emoji)"
    - "Footer renders as dark rounded-corner card with logo, nav links, contact info, Instagram, legal links"
    - "All new translation keys exist in all 4 locale files (es, en, fr, de)"
  artifacts:
    - path: "frontend/src/app/[locale]/page.tsx"
      provides: "Home page composition with all sections in correct order"
    - path: "frontend/src/components/home/VideoSection.tsx"
      provides: "4-video card grid from Directus web_videos"
    - path: "frontend/src/app/[locale]/layout.tsx"
      provides: "WhatsAppFAB globally placed after Footer"
    - path: "frontend/src/app/[locale]/sobre-vivaz/page.tsx"
      provides: "About page with TimelineSection and SVG icon cards"
    - path: "frontend/src/app/[locale]/contacto/page.tsx"
      provides: "Contact page with ContactSplit side-by-side layout"
    - path: "frontend/src/components/contact/ContactForm.tsx"
      provides: "Simplified contact form per CONTEXT decisions"
    - path: "frontend/src/components/layout/Footer.tsx"
      provides: "Corporate footer with dark card design"
  key_links:
    - from: "frontend/src/app/[locale]/page.tsx"
      to: "ContactSplit component"
      via: "import and render inline on home page"
      pattern: "ContactSplit"
    - from: "frontend/src/app/[locale]/page.tsx"
      to: "VideoSection component"
      via: "import and render with Directus data"
      pattern: "VideoSection"
    - from: "frontend/src/app/[locale]/layout.tsx"
      to: "WhatsAppFAB component"
      via: "import and render after Footer with brand WhatsApp number"
      pattern: "WhatsAppFAB"
    - from: "frontend/src/app/[locale]/page.tsx"
      to: "headers() geo-routing"
      via: "await headers() reads x-vivaz-market, passes as prop to HeroSection"
      pattern: "x-vivaz-market"
---

<objective>
Assemble the Home page (all sections in design order), About page (TimelineSection + SVG icons), and Contact page (ContactSplit side-by-side) with real Directus data, geo-routing, and briefing copy. Also add WhatsAppFAB globally to the locale layout, build VideoSection (new component), update the Footer to match the dark card design, and simplify ContactForm fields per CONTEXT decisions.

Purpose: These three pages plus global layout changes (WhatsAppFAB, Footer) form the brand-facing core of the site. The home page is the conversion funnel entry point; the contact page captures leads; the about page builds trust.

Output: 3 fully assembled pages + 1 new component (VideoSection) + updated Footer + simplified ContactForm + WhatsAppFAB in layout + new translation keys in all 4 locales.
</objective>

<execution_context>
@C:/Users/Piensaenweb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Piensaenweb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-page-assembly/04-CONTEXT.md
@.planning/phases/04-page-assembly/04-RESEARCH.md
@frontend/src/app/[locale]/page.tsx
@frontend/src/app/[locale]/layout.tsx
@frontend/src/app/[locale]/sobre-vivaz/page.tsx
@frontend/src/app/[locale]/contacto/page.tsx
@frontend/src/components/home/HeroSection.tsx
@frontend/src/components/contact/ContactForm.tsx
@frontend/src/components/contact/ContactSplit.tsx
@frontend/src/components/contact/ContactCards.tsx
@frontend/src/components/contact/WhatsAppFAB.tsx
@frontend/src/components/layout/Footer.tsx
@frontend/src/lib/directus.ts
@frontend/src/lib/types.ts
@frontend/src/messages/es.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global layout + VideoSection + Footer + ContactForm updates</name>
  <files>
    frontend/src/app/[locale]/layout.tsx
    frontend/src/components/home/VideoSection.tsx
    frontend/src/components/layout/Footer.tsx
    frontend/src/components/contact/ContactForm.tsx
    frontend/src/app/api/contact/route.ts
    frontend/src/lib/directus.ts
    frontend/src/lib/types.ts
    frontend/src/components/home/HeroSection.tsx
  </files>
  <action>
**1. WhatsAppFAB in locale layout (`layout.tsx`):**
- Import `WhatsAppFAB` from `@/components/contact/WhatsAppFAB` and `getBrandData` from `@/lib/directus`
- In `LocaleLayout`, fetch brand data with `try { brand = await getBrandData(); } catch { /* fallback */ }`
- Render `<WhatsAppFAB number={brand?.whatsapp ?? "+34618757580"} />` after `<Footer />` but still inside `<SmoothScroll>`
- This satisfies LEAD-04 (WhatsApp globally on all pages)

**2. WebVideo type + getVideos() data function:**
- In `frontend/src/lib/types.ts`, add:
  ```typescript
  export interface WebVideo {
    id: number;
    title: string;
    thumbnail: string | null;
    video_url: string;
    sort: number;
    status: string;
  }
  ```
- In `frontend/src/lib/directus.ts`, add `getVideos()` function:
  ```typescript
  export async function getVideos() {
    const items = await client.request(
      readItems("web_videos", {
        filter: { status: { _eq: "published" } },
        sort: ["sort"],
        limit: 4,
        fields: ["*"],
      })
    );
    return items as unknown as WebVideo[];
  }
  ```

**3. VideoSection component (`frontend/src/components/home/VideoSection.tsx`):**
- Create new Client Component (`"use client"`) that renders a 4-card responsive grid
- Props: `videos: WebVideo[]`, `translations: { title: string; watchVideo: string }`
- Grid: 1 col mobile, 2 col tablet (md:grid-cols-2), 4 col desktop (lg:grid-cols-4)
- Each card: `DirectusImage` thumbnail with rounded corners, play button overlay (SVG triangle in circle), title below
- Play button click: opens `video_url` in new tab (`window.open(url, '_blank')`) or a modal -- use `window.open` for simplicity
- Background: subtle topographic pattern using CSS repeating gradient or SVG pattern (CSS gradient is simpler -- use a subtle repeating linear-gradient in muted cream/sage tones to evoke contour lines)
- Section wrapped in dark green bg-primary section with cream text
- If `videos.length === 0`, render null (graceful empty state)

**4. HeroSection geo-adaptation:**
- HeroSection is currently a Client Component with `useTranslations("hero")`
- Add optional prop `market?: "national" | "export"` to HeroSection
- In the hero description area, conditionally show different translation keys based on market:
  - `market === "national"`: use `t("descriptionNational")` (performance/quality focus)
  - `market === "export"` or default: use `t("descriptionExport")` (regulation urgency focus)
- Add `hero.descriptionNational` and `hero.descriptionExport` keys to all 4 locale files
- `descriptionExport`: regulation urgency message about EU 2026 compliance
- `descriptionNational`: performance and 50+ years heritage message
- This satisfies HOME-04 and HOME-05

**5. Footer redesign (`frontend/src/components/layout/Footer.tsx`):**
- Read the current Footer.tsx first to understand what exists
- Redesign to match CONTEXT design: dark rounded-corner card (bg-primary rounded-3xl mx-4 mb-4 or similar)
- Content inside the rounded card: 4-column grid on desktop (1 col mobile)
  - Col 1: VIVAZ logo (text-based "VIVAZ" in heading font or SVG), brief tagline
  - Col 2: Navigation links (Productos, Tecnologia, Sobre Vivaz, Contacto) using Link from `@/i18n/navigation`
  - Col 3: Contact info (address from brand data, phone, email)
  - Col 4: Social links (Instagram icon link), legal links (Politica de Privacidad, Cookies)
- Bottom bar: copyright "2026 VIVAZ Clay Targets" in cream text
- Footer is a Server Component -- can use `getTranslations()` and `getBrandData()`
- Add `footer.*` translation keys to all 4 locales

**6. ContactForm simplification:**
- Read current ContactForm.tsx, then modify:
- Remove `company` and `interest` select fields per CONTEXT decision
- Add `apellidos` (last name) text input after name
- Add privacy checkbox with label: `t("contact.privacyConsent")` linked to `/politica-privacidad`
- Keep: name, email, phone, market select (Nacional/Internacional), message
- Submit button text: `t("contact.send")` ("ENVIAR")
- Update CrmLead type in types.ts: make `company` and `interest` optional (`company?: string`, `interest?: string`), add `apellidos?: string`
- Update API route (`/api/contact/route.ts`): handle optional company/interest, accept apellidos
- Add new translation keys: `contact.apellidos`, `contact.privacyConsent`, `contact.privacyLink` to all 4 locales
  </action>
  <verify>
    <automated>cd C:/Users/Piensaenweb/Documents/Claude/platos-vivaz/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>VideoSection renders with play overlay, Footer has dark card design, ContactForm has simplified fields</manual>
  </verify>
  <done>
    - WhatsAppFAB rendered globally in locale layout after Footer
    - WebVideo type and getVideos() function exist in lib
    - VideoSection component renders 4 video cards in responsive grid
    - HeroSection accepts market prop for geo-adapted copy
    - Footer redesigned as dark rounded-corner card with nav, contact, social links
    - ContactForm simplified: name, apellidos, email, phone, market, message, privacy checkbox
    - CrmLead type updated with optional company/interest and optional apellidos
    - All new translation keys added to all 4 locale files
  </done>
</task>

<task type="auto">
  <name>Task 2: Assemble Home, About, and Contact pages with all sections and data</name>
  <files>
    frontend/src/app/[locale]/page.tsx
    frontend/src/app/[locale]/sobre-vivaz/page.tsx
    frontend/src/app/[locale]/contacto/page.tsx
    frontend/src/messages/es.json
    frontend/src/messages/en.json
    frontend/src/messages/fr.json
    frontend/src/messages/de.json
  </files>
  <action>
**1. Home page assembly (`app/[locale]/page.tsx`):**
- Convert from sync default export to async Server Component (currently it's sync and has no data fetching)
- Import `headers` from `next/headers` to read `x-vivaz-market` header
- Import `getTranslations` from `next-intl/server` for the translations namespace
- Import `getVideos`, `getBrandData` from `@/lib/directus`
- Import existing components: HeroSection, ProductRangeStrip, WhyVivazGrid, ProductShowcase, CommitmentBanner
- Import new components: VideoSection from `@/components/home/VideoSection`, ContactSplit from `@/components/contact/ContactSplit`
- Remove: CTASection and ColorStrip imports (replaced by ContactSplit inline)
- Section order per CONTEXT decision:
  1. `<HeroSection market={market} />`
  2. `<ProductRangeStrip />`
  3. `<WhyVivazGrid />`
  4. `<ProductShowcase variant="natura" />`
  5. `<ProductShowcase variant="ecostar" />`
  6. `<VideoSection videos={videos} translations={videoTranslations} />`
  7. `<CommitmentBanner />`
  8. `<ContactSplit />`
- Fetch data:
  ```typescript
  const headersList = await headers();
  const market = (headersList.get("x-vivaz-market") ?? "export") as "national" | "export";
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: "home" });
  let videos: WebVideo[] = [];
  try { videos = await getVideos(); } catch { /* empty state */ }
  ```
- Build `videoTranslations` object: `{ title: t("videos.title"), watchVideo: t("videos.watchVideo") }`
- ContactSplit is already a Server Component that reads headers() internally -- just render `<ContactSplit />` at the bottom
- This satisfies HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, SUST-03, DESGN-01, DESGN-02

**2. About page assembly (`app/[locale]/sobre-vivaz/page.tsx`):**
- Read the current sobre-vivaz/page.tsx to understand existing structure
- Keep existing factory/heritage sections
- Import `TimelineSection` from `@/components/about/TimelineSection` (or wherever it was created in Phase 3)
- Import `getTranslations` from `next-intl/server`
- Build the timeline events array:
  ```typescript
  const timelineEvents = [
    { year: 1967, title: t("timeline.founded"), description: t("timeline.foundedDesc") },
    { year: 2001, title: t("timeline.pioneers"), description: t("timeline.pioneersDesc") },
    { year: 2026, title: t("timeline.regulation"), description: t("timeline.regulationDesc") },
  ];
  ```
- Render TimelineSection as the centerpiece (after hero, before values section)
- Replace emoji value cards (currently using emoji like target/bulb/leaf/handshake) with inline SVG icons:
  - Quality: target/crosshair SVG (concentric circles with center dot)
  - Innovation: lightbulb SVG
  - Sustainability: leaf SVG
  - Service: handshake SVG (two hands meeting)
- Keep the 4-column grid layout for values, just swap emoji for SVG
- Add timeline translation keys to all 4 locales: `about.timeline.founded`, `about.timeline.foundedDesc`, `about.timeline.pioneers`, `about.timeline.pioneersDesc`, `about.timeline.regulation`, `about.timeline.regulationDesc`
- This satisfies ABOUT-01, ABOUT-02

**3. Contact page assembly (`app/[locale]/contacto/page.tsx`):**
- Read the current contacto/page.tsx to understand existing structure
- Import `ContactSplit` from `@/components/contact/ContactSplit`
- The CONTEXT says Nacional and Internacional must be side-by-side on desktop (NOT stacked)
- ContactSplit already reads `x-vivaz-market` header internally via `await headers()`
- If ContactSplit currently renders cards stacked, the standalone contact page should wrap ContactCards in a layout that forces side-by-side: `<div className="grid lg:grid-cols-2 gap-8">` around the two ContactCard instances
- Alternative: pass a `layout="standalone"` prop to ContactSplit to switch the card layout from stacked to side-by-side
- Render the full page: PageHero at top, then ContactSplit with form + side-by-side cards
- This satisfies LEAD-01, LEAD-02, LEAD-03, LEAD-06

**4. Translation keys for all 3 pages:**
- Add ALL new keys to ALL 4 locale files (es.json, en.json, fr.json, de.json) simultaneously
- Keys needed:
  - `home.videos.title`, `home.videos.watchVideo`
  - `hero.descriptionNational`, `hero.descriptionExport` (from Task 1)
  - `about.timeline.founded`, `about.timeline.foundedDesc`, `about.timeline.pioneers`, `about.timeline.pioneersDesc`, `about.timeline.regulation`, `about.timeline.regulationDesc`
  - `footer.*` keys (navigation, legal, copyright)
  - `contact.apellidos`, `contact.privacyConsent`, `contact.privacyLink`, `contact.send`
- Spanish copy from briefing where available; English/French/German translations
- DESGN-04 satisfied by using briefing copy from translation files
  </action>
  <verify>
    <automated>cd C:/Users/Piensaenweb/Documents/Claude/platos-vivaz/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Home page shows all sections in correct order; About page has timeline + SVG icon cards; Contact page has side-by-side cards</manual>
  </verify>
  <done>
    - Home page renders all sections in design order: Hero (geo-adapted) -> ProductRangeStrip -> WhyVivaz -> NATURA -> ECO STAR -> Videos -> CommitmentBanner -> ContactSplit
    - About page has TimelineSection (1967/2001/2026) and SVG icon value cards (no emoji)
    - Contact page renders ContactSplit with Nacional/Internacional side-by-side on desktop
    - All new translation keys present in all 4 locale files
    - Briefing copy integrated where available
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. Home page imports and renders: HeroSection (with market prop), ProductRangeStrip, WhyVivazGrid, ProductShowcase x2, VideoSection, CommitmentBanner, ContactSplit
3. Layout.tsx includes WhatsAppFAB after Footer
4. About page renders TimelineSection with 3 milestone events
5. Contact page renders ContactSplit with side-by-side cards
6. ContactForm has 6 fields: name, apellidos, email, phone, market, message + privacy checkbox
7. All 4 locale files have identical key sets (no missing keys in any locale)
</verification>

<success_criteria>
- Home page section order matches CONTEXT exactly
- HeroSection displays geo-adapted copy based on x-vivaz-market header
- VideoSection component exists and renders 4 cards (or empty state)
- WhatsAppFAB visible on all pages
- Footer is a dark rounded-corner card with nav, contact, social, legal links
- Contact form simplified per CONTEXT (no company, no interest, added apellidos + privacy)
- About page has TimelineSection + SVG icons (no emoji)
- Zero TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-page-assembly/04-01-SUMMARY.md`
</output>
