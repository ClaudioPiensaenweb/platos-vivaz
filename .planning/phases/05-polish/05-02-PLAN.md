---
phase: 05-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - frontend/src/app/[locale]/sobre-vivaz/page.tsx
  - frontend/src/app/[locale]/tecnologia/page.tsx
  - frontend/src/app/[locale]/regulacion-2026/page.tsx
  - frontend/src/components/home/HeroSection.tsx
  - frontend/src/components/home/ProductShowcase.tsx
  - frontend/src/components/ui/PageHero.tsx
autonomous: true
requirements:
  - PERF-01
  - PERF-02

must_haves:
  truths:
    - "All hero/full-bleed fill images have sizes='100vw' prop set"
    - "Content images in 2-column grids have responsive sizes prop (e.g. '(max-width: 768px) 100vw, 50vw')"
    - "About, Technology, and Regulation pages have complete generateMetadata with description and openGraph"
    - "Headings on all pages use responsive text classes (e.g. text-2xl md:text-4xl) that do not overflow at 375px"
    - "German locale headings do not cause horizontal overflow at 375px viewport"
    - "All pages render without horizontal scrollbar at 375px viewport width"
  artifacts:
    - path: "frontend/src/components/ui/PageHero.tsx"
      provides: "sizes='100vw' on fill Image for LCP optimization"
      contains: "sizes"
    - path: "frontend/src/components/home/HeroSection.tsx"
      provides: "sizes='100vw' on hero background fill Image"
      contains: "sizes"
    - path: "frontend/src/app/[locale]/sobre-vivaz/page.tsx"
      provides: "Complete generateMetadata with description + OG, and responsive images with sizes"
      contains: "sharedOpenGraph"
    - path: "frontend/src/app/[locale]/tecnologia/page.tsx"
      provides: "Complete generateMetadata with description + OG, and responsive images with sizes"
      contains: "sharedOpenGraph"
    - path: "frontend/src/app/[locale]/regulacion-2026/page.tsx"
      provides: "Complete generateMetadata with description + OG"
      contains: "sharedOpenGraph"
  key_links:
    - from: "frontend/src/app/[locale]/sobre-vivaz/page.tsx"
      to: "frontend/src/lib/metadata.ts"
      via: "import sharedOpenGraph"
      pattern: "sharedOpenGraph"
    - from: "frontend/src/app/[locale]/sobre-vivaz/page.tsx"
      to: "frontend/src/messages/es.json"
      via: "t('metaDescription') reads about.metaDescription key added in Plan 01"
      pattern: "metaDescription"
---

<objective>
Optimize Core Web Vitals (image sizing, LCP) and ensure mobile responsiveness across all 8 pages. Complete remaining page metadata for about, technology, and regulation pages.

Purpose: Images without `sizes` cause oversized downloads on mobile. Missing responsive text classes cause heading overflow at narrow viewports. Three content-heavy pages still need generateMetadata completion. This plan closes all PERF-01 and PERF-02 gaps.
Output: All images have correct `sizes` props, all headings are responsive, all pages render cleanly at 375px, and the three remaining pages have complete SEO metadata.
</objective>

<execution_context>
@C:/Users/Piensaenweb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Piensaenweb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish/05-RESEARCH.md
@.planning/phases/05-polish/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sizes props to Image components and complete remaining page metadata</name>
  <files>
    frontend/src/components/home/HeroSection.tsx
    frontend/src/components/home/ProductShowcase.tsx
    frontend/src/components/ui/PageHero.tsx
    frontend/src/app/[locale]/sobre-vivaz/page.tsx
    frontend/src/app/[locale]/tecnologia/page.tsx
    frontend/src/app/[locale]/regulacion-2026/page.tsx
  </files>
  <action>
    **1. Add `sizes="100vw"` to fill images in shared components:**

    In `PageHero.tsx` — find the `<Image fill ...>` used for the background and add `sizes="100vw"`. This is a full-bleed hero image that always spans the viewport.

    In `HeroSection.tsx` — find any `<Image fill ...>` used for the hero background and add `sizes="100vw"`. If the hero uses a CSS background-image instead, skip this.

    **2. Add `sizes` to content images in `ProductShowcase.tsx`:**
    - Find `<Image>` components showing product images
    - Add `sizes="(max-width: 768px) 100vw, 50vw"` (images are in a 2-column grid on desktop, full-width on mobile)

    **3. Add `sizes` to content images in `sobre-vivaz/page.tsx`:**
    - History section images (2-column grid): `sizes="(max-width: 1024px) 100vw, 50vw"` (uses `lg:grid-cols-2`)
    - Factory section fill image: `sizes="100vw"` (full-bleed background)

    **4. Add `sizes` to content images in `tecnologia/page.tsx`:**
    - Pine Resin section image (702x478, 2-column grid): `sizes="(max-width: 1024px) 100vw, 50vw"`
    - Manufacturing Process section image: same pattern
    - Environmental Commitment section image: same pattern
    - REACH section background fill image: `sizes="100vw"`
    - SpotlightReveal images: leave as-is (SpotlightReveal is a client component with its own image handling)

    **5. Add `sizes` to images in `regulacion-2026/page.tsx`:**
    - CTA section background fill image: `sizes="100vw"`
    - Info cards check.svg: small icon, leave as-is (16x16)

    **6. Complete `generateMetadata` for the 3 remaining pages:**

    In `sobre-vivaz/page.tsx`:
    ```typescript
    import { sharedOpenGraph } from "@/lib/metadata";
    // Update generateMetadata return:
    const title = `${t("title")} | VIVAZ Clay Targets`;
    return {
      title,
      description: t("metaDescription"),
      openGraph: { ...sharedOpenGraph, title, description: t("metaDescription") },
    };
    ```

    In `tecnologia/page.tsx`:
    ```typescript
    import { sharedOpenGraph } from "@/lib/metadata";
    // Update generateMetadata return:
    const title = `${t("title")} | VIVAZ Clay Targets`;
    return {
      title,
      description: t("metaDescription"),
      openGraph: { ...sharedOpenGraph, title, description: t("metaDescription") },
    };
    ```

    In `regulacion-2026/page.tsx`:
    ```typescript
    import { sharedOpenGraph } from "@/lib/metadata";
    // Update generateMetadata return:
    const title = `${t("title")} | VIVAZ Clay Targets`;
    return {
      title,
      description: t("metaDescription"),
      openGraph: { ...sharedOpenGraph, title, description: t("metaDescription") },
    };
    ```

    NOTE: The `metaDescription` translation keys for about, technology, and regulation namespaces were added by Plan 05-01.
  </action>
  <verify>
    <automated>cd C:/Users/Piensaenweb/Documents/Claude/platos-vivaz/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Grep for `sizes=` in modified files to confirm all Image components have the prop</manual>
  </verify>
  <done>All Image components have appropriate sizes props. About, Technology, and Regulation pages have complete generateMetadata with description and openGraph.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile responsiveness sweep — heading overflow and German locale fixes</name>
  <files>
    frontend/src/app/[locale]/sobre-vivaz/page.tsx
    frontend/src/app/[locale]/tecnologia/page.tsx
    frontend/src/app/[locale]/regulacion-2026/page.tsx
    frontend/src/components/ui/PageHero.tsx
    frontend/src/components/home/HeroSection.tsx
  </files>
  <action>
    **1. Audit heading text classes for 375px viewport overflow:**

    Scan all h1/h2 headings in the 3 content pages (sobre-vivaz, tecnologia, regulacion-2026) and the PageHero component. For any heading using fixed large font sizes without responsive breakpoints, apply the `text-2xl md:text-3xl lg:text-[34px]` pattern.

    Common issues to fix:
    - `text-[28px] lg:text-[34px]` — fine for Spanish/English, may overflow on German at 375px. Check if `overflow-wrap: break-word` is needed on the container.
    - `text-[38px]` fixed sizes without responsive scaling

    **2. Add `overflow-wrap: break-word` to PageHero title container:**
    In `PageHero.tsx`, add `break-words` Tailwind class (or `overflow-wrap: break-word` via style) to the h1 element. This prevents German compound words like "Normkonformitat" from causing horizontal overflow. The class is `break-words` in Tailwind CSS v4.

    **3. Add `overflow-wrap: break-word` to HeroSection heading:**
    Same pattern — ensure the main h1/h2 in HeroSection has the `break-words` class for German locale safety.

    **4. Verify no horizontal overflow at 375px:**
    After making changes, verify that:
    - PageHero headings do not overflow at 375px
    - About page history/pioneer sections stack cleanly (lg:grid-cols-2 → single column)
    - Technology page SpotlightReveal and PAH chart fit within viewport
    - Regulation page countdown, timeline, and info cards stack properly
    - ComplianceMatrix uses existing `overflow-x-auto` for horizontal scroll (no changes needed)
    - FormatTabs on product pages use existing horizontal scroll (no changes needed)

    **5. Run build to verify everything compiles:**
    ```bash
    cd frontend && npx next build
    ```
    Check the build output for any warnings about image sizes or other optimization hints.
  </action>
  <verify>
    <automated>cd C:/Users/Piensaenweb/Documents/Claude/platos-vivaz/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify PageHero.tsx h1 has break-words class. Check that all heading elements have responsive text size classes.</manual>
  </verify>
  <done>All headings use responsive text classes. German compound words cannot cause horizontal overflow. All pages render cleanly at 375px viewport width. Build compiles without errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `grep -c "sizes=" frontend/src/components/ui/PageHero.tsx frontend/src/components/home/HeroSection.tsx frontend/src/components/home/ProductShowcase.tsx` — each file has at least 1 match
3. `grep "sharedOpenGraph" frontend/src/app/[locale]/sobre-vivaz/page.tsx frontend/src/app/[locale]/tecnologia/page.tsx frontend/src/app/[locale]/regulacion-2026/page.tsx` — 3 matches
4. `grep "break-words" frontend/src/components/ui/PageHero.tsx` — at least 1 match
5. `grep "metaDescription" frontend/src/app/[locale]/sobre-vivaz/page.tsx frontend/src/app/[locale]/tecnologia/page.tsx frontend/src/app/[locale]/regulacion-2026/page.tsx` — 3 matches
</verification>

<success_criteria>
- All Image components have appropriate sizes prop for their layout context
- All hero fill images have sizes="100vw"
- Content images in 2-column grids have responsive sizes with breakpoints
- All headings use responsive text classes that scale down at 375px
- PageHero and HeroSection headings have break-words for German locale safety
- About, Technology, and Regulation pages have complete generateMetadata
- TypeScript compilation passes
- Build output has no image-related warnings
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-02-SUMMARY.md`
</output>
