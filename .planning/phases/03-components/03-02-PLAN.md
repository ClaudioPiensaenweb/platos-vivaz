---
phase: 03-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/product/TechSpecGrid.tsx
  - frontend/src/components/about/TimelineSection.tsx
  - frontend/src/components/about/StatCard.tsx
  - frontend/src/components/contact/ContactSplit.tsx
  - frontend/src/components/contact/ContactCards.tsx
  - frontend/src/components/contact/WhatsAppFAB.tsx
  - frontend/src/components/blog/BlogCard.tsx
  - frontend/src/components/blog/BlogHero.tsx
  - frontend/src/components/blog/BlogContent.tsx
  - frontend/src/components/technology/CertBadgeRow.tsx
autonomous: true
requirements:
  - PROD-04
  - SUST-05
  - ABOUT-03

must_haves:
  truths:
    - "TechSpecGrid displays PAH level prominently alongside weight, diameter, material, and other specs"
    - "TechSpecGrid receives a Product object and renders a responsive grid of spec cells"
    - "TimelineSection renders 1967, 2001, and 2026 milestones as a horizontal timeline on desktop and vertical on mobile"
    - "TimelineSection is a props-driven component accepting an events array — reusable between technology and about pages"
    - "ContactSplit renders a 50/50 grid with form left and contact cards right"
    - "ContactSplit reads x-vivaz-market header and highlights the matching card"
    - "WhatsApp FAB has aria-label for accessibility and is a fixed-position button"
    - "BlogCard, BlogHero, BlogContent are standalone Server Components for blog rendering"
  artifacts:
    - path: "frontend/src/components/product/TechSpecGrid.tsx"
      provides: "Product technical specifications grid with PAH prominence"
      contains: "pah_level"
    - path: "frontend/src/components/about/TimelineSection.tsx"
      provides: "Horizontal/vertical milestone timeline"
      contains: "events"
    - path: "frontend/src/components/contact/ContactSplit.tsx"
      provides: "50/50 contact layout with geo-routing"
      contains: "x-vivaz-market"
    - path: "frontend/src/components/contact/WhatsAppFAB.tsx"
      provides: "Fixed WhatsApp floating action button"
      contains: "wa.me"
    - path: "frontend/src/components/blog/BlogCard.tsx"
      provides: "Blog listing card component"
      contains: "BlogPost"
    - path: "frontend/src/components/blog/BlogHero.tsx"
      provides: "Blog post hero header"
      contains: "BlogPost"
    - path: "frontend/src/components/blog/BlogContent.tsx"
      provides: "Sanitized HTML blog body"
      contains: "sanitizeHtml"
  key_links:
    - from: "frontend/src/components/product/TechSpecGrid.tsx"
      to: "frontend/src/lib/types.ts"
      via: "Product type import"
      pattern: "import.*Product.*from.*types"
    - from: "frontend/src/components/contact/ContactSplit.tsx"
      to: "next/headers"
      via: "await headers() for geo-routing"
      pattern: "headers.*x-vivaz-market"
    - from: "frontend/src/components/blog/BlogContent.tsx"
      to: "frontend/src/lib/sanitize.ts"
      via: "sanitizeHtml import for XSS protection"
      pattern: "import.*sanitizeHtml"
---

<objective>
Build the feature-specific components: TechSpecGrid for product detail pages, TimelineSection for brand history, ContactSplit with geo-routing, WhatsApp FAB, blog components, and remaining shared primitives (CertBadgeRow, StatCard).

Purpose: Complete all remaining Phase 3 components so Phase 4 can compose full pages by importing these components with real data and translations.

Output: TechSpecGrid, TimelineSection, StatCard, ContactSplit, ContactCards, WhatsAppFAB, BlogCard, BlogHero, BlogContent, CertBadgeRow — all typed, accessible, and ready for page assembly.
</objective>

<execution_context>
@C:/Users/Piensaenweb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Piensaenweb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-components/03-RESEARCH.md

@frontend/src/lib/types.ts
@frontend/src/lib/directus.ts
@frontend/src/lib/sanitize.ts
@frontend/src/components/product/ProductCard.tsx
@frontend/src/components/product/LogisticsTable.tsx
@frontend/src/components/product/DisciplineBadge.tsx
@frontend/src/components/ui/InView.tsx
@frontend/src/components/ui/SectionHeader.tsx
@frontend/src/app/[locale]/noticias/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build TechSpecGrid, TimelineSection, and StatCard components</name>
  <files>
    frontend/src/components/product/TechSpecGrid.tsx
    frontend/src/components/about/TimelineSection.tsx
    frontend/src/components/about/StatCard.tsx
  </files>
  <action>
**TechSpecGrid.tsx** — Server Component (no "use client") for PROD-04:
1. Props interface: `{ product: Product; translations: { pahLevel: string; diameter: string; weight: string; height: string; material: string; color: string; resinContent: string; issfApproved: string; yes: string; no: string; } }`
2. Display PAH level PROMINENTLY as the first/largest cell — use `product.pah_level` with color coding: "0 mg/kg - Free" gets `bg-primary/10 text-primary` (green), "< 50 mg/kg - Compliant" gets `bg-warning/10 text-warning` (amber).
3. Responsive grid: `grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4` — each cell is a small card with label (muted text-sm) and value (font-semibold).
4. Fields rendered: PAH level (prominent), diameter_mm (with "mm" suffix), weight_g (with "g" suffix), height_mm (with "mm" suffix), material, color, resin_pct (with "%" suffix), issf_approved (yes/no badge).
5. Null handling: skip cells where value is null (some products may not have all fields).
6. PAH cell spans full width on mobile (`col-span-2 md:col-span-1`) for visual prominence.
7. Below the grid: render DisciplineBadge row if `product.disciplines` exists and is non-empty — import DisciplineBadge from existing component.
8. Styling: use `bg-cream/50 rounded-xl p-6` wrapper matching existing ProductDetail section style.

**TimelineSection.tsx** — Server Component (no "use client") for SUST-05 and ABOUT-03:
1. Props interface: `{ events: Array<{ year: number; title: string; description: string; }>; className?: string; }` — props-driven, reusable between tech and about pages with different copy.
2. Layout: horizontal on `md+` (flex-row with connecting line between dots), vertical on mobile (flex-col with vertical line).
3. Each event: circle dot (primary color) with year inside or above, title text bold, description text muted below.
4. Connecting line: thin line (`bg-primary/20`) connecting the dots — horizontal on desktop, vertical on mobile.
5. Responsive: use `flex flex-col md:flex-row` with `flex-1` on each event for equal spacing.
6. Year styling: `font-heading text-2xl font-bold text-primary` — the year is the visual anchor.
7. The component does NOT hardcode 1967/2001/2026 — it receives events array from the consuming page. Phase 4 will supply the actual content.
8. Wrap in InView for fade-in-up entrance animation (import InView from `@/components/ui/InView`).

**StatCard.tsx** — Server Component for shared stats display:
1. Props interface: `{ value: string; label: string; icon?: ReactNode; className?: string; }`
2. Simple card: icon (optional) at top, value in large heading font, label in muted body font.
3. Styling: `bg-white rounded-xl p-6 text-center shadow-sm` — minimal, clean.
4. Used on about page for key statistics (e.g., "1967 Founded", "25+ Years", "4 Locales").

**Directory creation:** Create `frontend/src/components/about/` directory (new).
  </action>
  <verify>
    <automated>cd C:/Users/Piensaenweb/Documents/Claude/platos-vivaz/frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | head -30</automated>
    <manual>TechSpecGrid renders product specs with PAH prominent; TimelineSection renders events horizontally on desktop and vertically on mobile</manual>
  </verify>
  <done>TechSpecGrid renders product specs in a responsive grid with PAH level as the most prominent cell. TimelineSection accepts events array and renders horizontal/vertical timeline. StatCard renders value/label pairs. All three are Server Components with proper TypeScript interfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Build ContactSplit, WhatsAppFAB, CertBadgeRow, and blog components</name>
  <files>
    frontend/src/components/contact/ContactSplit.tsx
    frontend/src/components/contact/ContactCards.tsx
    frontend/src/components/contact/WhatsAppFAB.tsx
    frontend/src/components/blog/BlogCard.tsx
    frontend/src/components/blog/BlogHero.tsx
    frontend/src/components/blog/BlogContent.tsx
    frontend/src/components/technology/CertBadgeRow.tsx
  </files>
  <action>
**ContactSplit.tsx** — Server Component (no "use client") per CONTEXT.md locked decision:
1. Import `headers` from `next/headers`. Call `await headers()` (async — Next.js 16 requires await).
2. Read `x-vivaz-market` header: `headersList.get("x-vivaz-market") ?? "export"` — cast to `"national" | "export"`.
3. Props interface: `{ translations: { national: string; international: string; phone: string; email: string; formTitle: string; } }` — contact label translations from parent page.
4. Render `<div className="grid gap-12 lg:grid-cols-2">` — left: existing ContactForm component, right: ContactCards component.
5. Pass `highlightedMarket={market}` and translations to ContactCards (Client Component for highlight state).
6. On mobile (`<lg`): columns stack, form first, then cards below.

**ContactCards.tsx** — Client Component ("use client") for interactive highlight:
1. Props: `{ highlightedMarket: "national" | "export"; translations: { national: string; international: string; phone: string; email: string; } }`
2. Two stacked cards: top "Nacional" with Spanish phone (+34-618-757-580) and email (export@platosvivaz.com), bottom "Internacional / Export" with export phone (+34-606-172-746) and email (sales@vivazclaytargets.com). Note: the actual phone/email values will come from BrandData in Phase 4 — for now use placeholders that match the BrandData shape.
3. Highlighted card (matching highlightedMarket): subtle `ring-2 ring-primary/30 bg-primary/5` border/background. Non-highlighted card: standard `bg-white border border-gray-200`.
4. Each card: icon (phone SVG, email SVG), label, clickable phone (`tel:` href) and email (`mailto:` href).
5. Both cards ALWAYS visible — never hide the non-matching card.

**WhatsAppFAB.tsx** — Client Component ("use client") for fixed button:
1. Props: `{ number: string; ariaLabel?: string; }` — phone number for wa.me link.
2. Render `<a href="https://wa.me/{sanitized_number}" target="_blank" rel="noopener noreferrer" aria-label={ariaLabel || "Contactar por WhatsApp"}>` — aria-label is REQUIRED for icon-only button accessibility.
3. Fixed position: `fixed bottom-6 right-6 z-50`.
4. Styling: `flex h-14 w-14 items-center justify-center rounded-full bg-[#25D366] shadow-lg transition-transform duration-200 hover:scale-110 focus:outline-none focus:ring-2 focus:ring-[#25D366] focus:ring-offset-2`.
5. Inline WhatsApp SVG icon (white, ~24x24px) — standard WhatsApp logo path.
6. Phone number sanitization: `number.replace(/[^0-9]/g, "")` to strip formatting.

**BlogCard.tsx** — Server Component replacing inline article from noticias/page.tsx:
1. Props: `{ post: BlogPost; locale: string; }` — receives BlogPost type from types.ts.
2. Renders: image (via assetUrl + DirectusImage), title, excerpt, published date, category badge, link to `/noticias/{slug}`.
3. Uses `@/i18n/navigation` Link for locale-aware routing.
4. Image: use `assetUrl(post.image, { width: 600, format: "webp" })` pattern.
5. Translation-aware: use `post.translations` to find matching locale content for title/excerpt.
6. Styling: card with rounded corners, shadow hover state, image at top.

**BlogHero.tsx** — Server Component for blog post detail header:
1. Props: `{ post: BlogPost; locale: string; }` — renders the hero area of a blog detail page.
2. Renders: full-width image, title overlay, published date, category.
3. Uses same locale-aware translation extraction as BlogCard.

**BlogContent.tsx** — Server Component for blog post body:
1. Props: `{ content: string; }` — receives the raw HTML content from Directus.
2. Import `sanitizeHtml` from `@/lib/sanitize` — MUST sanitize before rendering.
3. Render with `<div className="prose prose-lg max-w-none" dangerouslySetInnerHTML={{ __html: sanitizeHtml(content) }} />`.
4. Uses @tailwindcss/typography prose classes for rich text styling (installed in Phase 1).

**CertBadgeRow.tsx** — Server Component for certification badge display:
1. Props: `{ certifications: string[]; className?: string; }` — array of certification names.
2. Renders horizontal row of badge pills (similar to DisciplineBadge styling).
3. Each badge: `bg-primary/10 text-primary text-xs font-medium px-3 py-1 rounded-full`.
4. Flex wrap for responsive behavior.

**Directory creation:** Create `frontend/src/components/blog/` directory (new). `frontend/src/components/technology/` should already exist from Plan 01, but create if missing.
  </action>
  <verify>
    <automated>cd C:/Users/Piensaenweb/Documents/Claude/platos-vivaz/frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | head -30</automated>
    <manual>ContactSplit renders 50/50 grid; WhatsAppFAB renders fixed green button; BlogCard renders post card with image</manual>
  </verify>
  <done>ContactSplit reads x-vivaz-market header and passes to ContactCards for highlight. WhatsAppFAB has aria-label and fixed positioning. BlogCard/BlogHero/BlogContent handle locale-aware blog rendering with sanitized HTML. CertBadgeRow renders certification pills. All components type-check with zero errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — all new components type-check correctly
2. `npm run lint` passes — no ESLint errors
3. TechSpecGrid.tsx imports Product type from `@/lib/types` and renders PAH cell prominently
4. TimelineSection.tsx accepts `events` array prop — not hardcoded content
5. ContactSplit.tsx uses `await headers()` (async) to read `x-vivaz-market`
6. WhatsAppFAB.tsx has `aria-label` attribute on the anchor element
7. BlogContent.tsx uses `sanitizeHtml()` before `dangerouslySetInnerHTML`
8. No `useTranslations()` in any Client Component — translations passed as props
9. New directories `about/` and `blog/` created under `src/components/`
</verification>

<success_criteria>
- TechSpecGrid renders product specs with PAH level as the most visually prominent cell
- TimelineSection is reusable — same component serves SUST-05 (tech page) and ABOUT-03 (about page)
- ContactSplit renders 50/50 layout with form + geo-highlighted contact cards
- WhatsApp FAB is accessible (aria-label) and fixed-position
- Blog components handle locale-aware content rendering with sanitized HTML
- CertBadgeRow and StatCard are clean, minimal primitives
- All new files are Server Components except ContactCards and WhatsAppFAB (which need client interactivity)
- Zero TypeScript errors across all new and existing files
</success_criteria>

<output>
After completion, create `.planning/phases/03-components/03-02-SUMMARY.md`
</output>
