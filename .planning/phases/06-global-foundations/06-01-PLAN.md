---
phase: 06-global-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/Navbar.tsx
  - frontend/src/components/layout/NavbarClient.tsx
  - frontend/src/messages/es.json
  - frontend/src/messages/en.json
  - frontend/src/messages/fr.json
  - frontend/src/messages/de.json
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "Navbar displays 'Tecnología y sostenibilidad' (lowercase s) in Spanish locale"
    - "Navbar displays equivalent translated label for technology+sustainability in en/fr/de"
    - "Navbar shows a single 'Noticias / Regulación 2026' combined link in all 4 locales"
    - "Instagram icon link appears in the desktop navbar next to the last nav item"
    - "Instagram icon link appears in the mobile menu overlay"
    - "Instagram link points to the real Vivaz Instagram profile URL"
  artifacts:
    - path: "frontend/src/components/layout/Navbar.tsx"
      provides: "Navbar server component with correct menu items and Instagram link"
    - path: "frontend/src/messages/es.json"
      provides: "Spanish nav translations with correct casing"
      contains: "Tecnología y sostenibilidad"
  key_links:
    - from: "frontend/src/components/layout/Navbar.tsx"
      to: "frontend/src/messages/{locale}.json"
      via: "useTranslations('nav')"
      pattern: "t\\(\"technology\"\\)"
---

<objective>
Verify and finalize navbar restructure: correct translation casing for the technology link, confirm the combined Noticias/Regulación link works across all 4 locales, and ensure the Instagram icon link uses the real Vivaz profile URL.

Purpose: The v1.0 navbar already implemented most of the NAV requirements during Phase 1/4 work. This plan verifies correctness, fixes the Spanish casing to match the brand standard (lowercase "sostenibilidad"), and updates the placeholder Instagram URL to the real Vivaz profile.

Output: Navbar renders correctly in all 4 locales with the renamed technology link, combined news/regulation link, and functional Instagram icon.
</objective>

<execution_context>
@C:/Users/Piensaenweb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Piensaenweb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/layout/Navbar.tsx
@frontend/src/components/layout/NavbarClient.tsx
@frontend/src/messages/es.json
@frontend/src/messages/en.json
@frontend/src/messages/fr.json
@frontend/src/messages/de.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix nav translation casing and Instagram URL across all locales</name>
  <files>
    frontend/src/messages/es.json
    frontend/src/messages/en.json
    frontend/src/messages/fr.json
    frontend/src/messages/de.json
    frontend/src/components/layout/Navbar.tsx
    frontend/src/components/layout/NavbarClient.tsx
  </files>
  <action>
The v1.0 codebase already has most NAV requirements implemented. This task makes targeted fixes:

1. **NAV-01 — Translation casing fix (es.json):**
   - Change `"technology": "Tecnología y Sostenibilidad"` to `"technology": "Tecnología y sostenibilidad"` (lowercase "s" in "sostenibilidad" — this is a sentence-case brand decision, not a title).
   - Verify en.json, fr.json, de.json equivalents are correct and keep their current casing (English/German use title case by convention, French uses sentence case). The key `technology` already contains the full "Technology & Sustainability" label in each locale — confirm no changes needed for en/fr/de.

2. **NAV-02 — Verify combined link:**
   - Confirm that the `menuItems` array in `Navbar.tsx` has exactly 5 items: Productos, Tecnología y sostenibilidad, Sobre Vivaz, Noticias / Regulación 2026, Contacto.
   - Confirm the `news` key maps to `href: "/regulacion-2026"` (single combined destination). No separate "Noticias" link should exist.
   - Confirm all 4 locale files have `nav.news` key with the combined label format (already present).

3. **NAV-03 — Instagram URL:**
   - In `Navbar.tsx`, change the desktop Instagram link `href="https://instagram.com"` to `href="https://www.instagram.com/vivaz_claytargets/"` (the official Vivaz Instagram profile).
   - In `NavbarClient.tsx`, change the mobile menu Instagram link `href="https://instagram.com"` to `href="https://www.instagram.com/vivaz_claytargets/"`.
   - Both links must have `target="_blank"` and `rel="noopener noreferrer"` (already present).

Do NOT restructure the Navbar component layout — the existing split-bar design (dark logo section + green links section + Instagram + language dropdown) is correct and will be preserved.
  </action>
  <verify>
    <automated>cd frontend && npx next build 2>&1 | tail -5</automated>
    <manual>Run dev server (`npm run dev`), visit localhost:3000. Check navbar shows "Tecnología y sostenibilidad" (ES), switch to EN and confirm "Technology &amp; Sustainability", switch to FR confirm "Technologie et Durabilité", switch to DE confirm "Technologie &amp; Nachhaltigkeit". Verify Instagram icon is visible on desktop navbar and mobile menu, clicking opens instagram.com/vivaz_claytargets/ in new tab.</manual>
  </verify>
  <done>
    - Spanish locale navbar shows "Tecnología y sostenibilidad" (lowercase s)
    - All 4 locales show correct technology+sustainability label
    - All 4 locales show combined "Noticias / Regulación 2026" as single link
    - Instagram icon in desktop navbar links to https://www.instagram.com/vivaz_claytargets/
    - Instagram icon in mobile menu links to https://www.instagram.com/vivaz_claytargets/
    - `next build` compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx next build` completes without errors
2. Grep all 4 locale files for `nav.technology` — each returns the full sustainability label
3. Grep all 4 locale files for `nav.news` — each returns the combined Noticias/Regulación label
4. Grep Navbar.tsx and NavbarClient.tsx for `instagram.com` — both return the full profile URL
5. No duplicate or separate "Noticias" link exists in Navbar.tsx menuItems array
</verification>

<success_criteria>
- Navbar shows "Tecnología y sostenibilidad" in ES locale (lowercase s)
- Navbar shows combined "Noticias / Regulación 2026" link in all 4 locales
- Instagram icon link points to real Vivaz profile in both desktop and mobile views
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-global-foundations/06-01-SUMMARY.md`
</output>
